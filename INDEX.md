# INDEX.md

Authoritative file map + purpose index for this repository. **Check here first** before grepping/globbing/searching — every code file below has a one-line description of what it does, so you can usually jump straight to the right file.

Complete file index for this repository (486 files, excluding `node_modules/`, `backend/node_modules/`, `.git/`, and `.claude/worktrees/` leftover git worktrees). Generated from a full repo walk.

---

- **.claude/**
  - settings.local.json — local Claude Code permissions/settings for this repo (not synced)
- **.github/**
  - **workflows/**
    - ci.yml — GitHub Actions: on push/PR installs backend deps, runs `precheck`, runs `backend/test/*.test.js`
    - db-backup.yml — GitHub Actions: daily encrypted `pg_dump` of production DB, uploaded as a 90-day artifact (independent of Supabase's own backup plan); needs `DATABASE_URL` + `BACKUP_ENCRYPTION_PASSPHRASE` repo secrets
- **.vscode/**
  - settings.json — Live Server port config for local static preview
- **backend/**
  - **js/**
    - convert-md-to-json.js — one-off script: merges translated .md console content into per-language JSON structure
    - import-consoles.js — imports `frontend/js/data/consoles-{lang}.json` into Postgres `consoles_translations`. **No longer run automatically (2026-09-06)** — `consoles_translations` is now the live, admin-editable source of truth (see `routes/consoles.js`'s `PUT /:id` and CLAUDE.md's "Console encyclopedia data" note); this script is a manual-only seed/disaster-recovery tool now (`npm run import-consoles`), and running it will blindly overwrite (`ON CONFLICT ... DO UPDATE`) any admin edits made since the JSON was last exported.
    - reset-database.js — wipes user/session/token data for a clean public deployment (real logic behind `npm run reset-db`)
  - **middleware/**
    - adminOnly.js — Express middleware gating a route to `role === 'admin'`; must run after `authRequired`
    - auth.js — `authRequired`/`authOptional`: validates JWT, falls back to hashed session-cookie lookup, populates `req.user`; `authRequired` rejects with 401 if `users.is_banned`
  - **providers/**
    - EbayProvider.js — eBay marketplace integration: OAuth, listings fetch, normalization against real eBay APIs. `authenticate()`/`refreshToken()` return `expiresAt` as a real `Date` object (fixed 2026-07-23 — was a raw `Date.now() + ms` number, which `pg` serializes to its literal digit string and Postgres then fails to parse as a `TIMESTAMP` with "date/time field value out of range"; `routes/ebay.js`'s own separate OAuth callback already wrapped this correctly, only this provider class's methods had the bug, and only `refreshToken()` was actually being hit in production since it's what the periodic sync job calls)
    - MarketplaceProvider.js — abstract base class defining the contract every marketplace provider must implement
    - OlxProvider.js — OLX marketplace integration: OAuth + listings fetch/normalization against OLX Group API. Same `expiresAt` → real `Date` object fix as EbayProvider.js (2026-07-23), applied here for consistency even though no OLX account has ever actually connected
  - **routes/**
    - achievements.js — GET own achievements (`/`) and another user's by username (`/user/:username`)
    - admin-stats.js — `GET /api/admin/stats` (`adminOnly`): growth/activity dashboard aggregate (total users/listings/threads/replies, signups per day for 30 days, `activeUsers7d`/`30d` approximated from `user_sessions.last_activity`, listings by status). `POST /users/:id/ban` also calls `reports.js`'s `pushBanNotice()` (2026-09-01, new) — same immediate-logout push as a report-triggered ban.
    - articles.js — `/api/articles`: admin-written blog posts shown in a new "Articles" section on home.html and on the public `articles.html`/`article.html` pages. `GET /` (public) lists published articles newest-first (`limit`/`offset`); `GET /:slug` (`authOptional` — an admin can preview their own unpublished drafts, everyone else gets 404) returns one article and increments `views`; `POST /`/`PUT /:id`/`DELETE /:id` are `adminOnly`. Slugs are auto-generated from the title (`slugify()` + `uniqueSlug()` collision suffixing), re-slugged on `PUT` only if the title actually changed. Cover images go through `POST /api/uploads/presign` (`kind: 'article'`, admin-gated there too), validated to start with `articles/image/${userId}/`; `content_html` is trusted raw HTML (admin-only writer, same trust model as `lessons.content_html` — rendered via `.innerHTML` on the frontend, no sanitization).
    - leaderboard.js — `GET /api/leaderboard` (public, no auth): top users by XP, respects `show_stats` privacy flag; includes the caller's own rank if logged in (`authOptional`)
    - auth.js — registration (Turnstile CAPTCHA + disposable-email block), login + 2FA, email verification, password reset, profile, avatar upload; largest route file; `POST /login` rejects banned accounts with `account_banned`-style message before issuing a token. `sanitizeUser()` includes `muted_until` (2026-09-01, new, self only) — powers the app's chat-composer mute block, see `MeResponse.muted_until` on the app repo. Avatar upload (`POST /me/avatar`) — multer memory storage → magic-byte check → sharp resize/re-encode to WebP → since 2026-07-23 uploaded via `objectStorage.js`'s `uploadBuffer()` to the R2 bucket at a fixed `avatars/${userId}.webp` key (was Supabase Storage's `avatars` bucket before; `supabaseStorage.js`/`supabaseAdmin` is still used elsewhere by workshop.js, just no longer for avatars). `DELETE /me/avatar` calls `deleteAttachment()` on that same key.
    - chat.js — community/global chat: paginated message retrieval + posting with per-user rate-limit cooldown. `POST /messages` accepts an optional attachment (`attachment_key`/`attachment_type` `'image'|'voice'|'sticker'`/`attachment_size`/`attachment_duration_ms`, same shape as dm.js) — `attachment_type` gates the required `chat/{images,voice,stickers}/{userId}/` key prefix. Only `sticker` is actually used here today (Android's general chat only supports sending stickers, not photo/voice — see `ui/chat/ChatViewModel.kt` on the app repo). `POST /messages` now also rejects a muted user (`isMuted(req.user)`, `utils/moderation.js`) — the one route among forum.js/marketplace.js/dm.js/chat.js that never had this check (2026-09-01)
    - community-photos.js — `/api/community/photos`: metadata CRUD for the Community "Photos" tab gallery (`POST` validates `imageKey` prefix from a presigned upload, `GET` paginated newest-first, `DELETE` owner-only); image bytes go straight to object storage via `POST /api/uploads/presign` (`kind: 'gallery'`), never through this route
    - console-models.js — `/api/console-models` (new 2026-09-06, Phase 3 of the site-wide admin-editing task): the hardware model/revision directory (mfr/console/code/note), moved here from a static `frontend/js/data/console-models.js` array. Self-inits `console_models` (`id`/`mfr`/`console`/`code` UNIQUE/`note`/`updated_by`/`updated_at`), same pattern as `console-tutorials.js`. `GET /` (public) returns all rows ordered by `id` (insertion order, matching the original 209-entry array's order — all of it migrated as-is via a one-off script). `POST /` (`authRequired`+`adminOnly`) adds a new model; `PUT /:id` (same guard) edits mfr/console/code/note — if `code` changes, cascades the rename into `console_tutorials`/`console_mod_tutorials` (both keyed by `model_code`) inside the same transaction (`pool.connect()`/`BEGIN`/`COMMIT`/`ROLLBACK`), so renaming a model's code can't silently orphan its existing disassembly/modding-guide content. Both write routes 409 on a duplicate `code` (`err.code === '23505'`). `MOD_OPTIONS` (the flash-type/firmware pick-list) stays a static export in the frontend data file, untouched by this move.
    - console-tutorials.js — `/api/console-tutorials/:code`: content for console-model.html, keyed by the same `code` as `frontend/js/data/console-models.js`. TWO separate tables now: `console_tutorials` holds the single-row-per-model disassembly tutorial (`title`/`intro`/`steps`, `[{heading, description, image_url}]`) — `GET /:code` (public), `PUT /:code`/`DELETE /:code` (`authRequired`+`adminOnly`); its legacy `mod_title`/`mod_intro`/`mod_steps` columns are unused/superseded, left in place per the repo's never-drop-columns convention. `console_mod_tutorials` holds MULTIPLE modding-guide rows per model, one per `(model_code, flash_type, firmware_version)` — e.g. PS3 `CECHJ` can have an independent `NOR`/`4.93` write-up unrelated to any other combo, or PS2 `SCPH-70004` a `"Memory Card (softmod, no disc-swap, no donor card)"`/`"FMCB 1.966"` write-up (`flash_type` widened `VARCHAR(40)`→`VARCHAR(80)` 2026-09-06 for this — PS2 softmod combos describe the install medium in prose rather than PS3's short NOR/NAND labels this column was designed for). `GET /:code/mod?lang=` (public, `lang` defaults `en`) returns every combo's full content in one call (so console-model.js can populate both selector dropdowns and switch client-side with no extra round-trips) — `id`/`flash_type`/`firmware_version`/`title`/`intro`/`steps`/`updated_at`/`has_translation` per combo, with `title`/`intro`/`steps` LEFT JOINed + `COALESCE`d against **`console_mod_tutorials_translations`** (2026-09-06, new — mirrors `lesson_translations`: `tutorial_id` FK→`console_mod_tutorials.id` `ON DELETE CASCADE`, `lang`, `title`/`intro`/`steps`, `updated_by`, `updated_at`, PK `(lang, tutorial_id)`) so a combo with no translation for the requested language just serves its EN canonical fields; `has_translation` (`t.tutorial_id IS NOT NULL`) tells the caller whether that's a real translation or just the EN fallback, without a second round-trip. `PUT /:code/mod/:flashType/:version` (upsert, serves both "add new combo" and "edit existing", writes the EN canonical row) and `DELETE /:code/mod/:flashType/:version` are `authRequired`+`adminOnly`; `PUT`/`DELETE /:code/mod/:flashType/:version/translations/:lang` (also `authRequired`+`adminOnly`) upsert/remove one language's override for one combo (404s if the EN combo doesn't exist yet — translations hang off its `id`). **Admin translation UI (2026-09-06)**: `console-model.js`'s modding-guide admin controls now branch on `I18nModule.lang` — browsing in EN shows the original Edit/Add-combo controls (unchanged); browsing in any other language shows a single "Edit/Add translation" button (label picked from the current combo's `has_translation`) that opens the same step editor pre-filled with the effective (translated-or-EN-fallback) content, saves to the `.../translations/:lang` endpoint instead of the base one, and — via a "Remove translation" button inside that editor, shown only when a real override exists — deletes just that language's row (re-fetches the combo list afterwards so the view falls back to EN correctly, since the local cache no longer knows the EN values once overwritten by a translation). "Add new combo" stays EN-only (flash type/firmware version are structural identifiers, not translatable content) — hidden entirely outside EN. All three tables self-init on load (`CREATE TABLE IF NOT EXISTS`, same self-init pattern as courses.js's `lesson_reactions`). Step photos go through `POST /api/uploads/presign` with `kind: 'tutorial'` (admin-gated there too, see uploads.js). **PS2 modding-guide content — full rollout (2026-09-06)**: every PS2 model code now has a modding guide EXCEPT `SCPH-10000`/`SCPH-15000` (Japan launch-only, 2000; the OpenTuna/Fortuna exploit's own documented compatibility explicitly starts one step later at `SCPH-18000`, and no other bootstrap method is confirmed for these two — deliberately left blank rather than guessed). Three flash_type families, all inserted directly via SQL (`backend/db.js`'s `pool` from one-off scripts in the same session, not through the admin PUT UI, same precedent as the PS3 CECHJ guide) rather than hand-written per model:
  - **`"Memory Card (softmod, no disc-swap, no donor card)"` / `"FMCB 1.966"`** (18 codes: `SCPH-70000x`–`SCPH-79000x` Slims, including `SCPH-70011`/`SCPH-77003` found in the model-list gap-fill below) — FreeDVDBoot → FreeMcBoot Installer → Open PS2 Loader (USB mode). Same 13 steps verbatim per model except the exact code substituted throughout and a "Verified on" step that's explicit about what was actually hands-on tested (`SCPH-70004` only) vs. same-hardware-family inference (the other 17).
  - **`"Memory Card (OpenTuna/Fortuna exploit, no DVD drive needed)"` / `"FMCB 1.966"`** (7 codes: `SCPH-9000x` — including `SCPH-90010` — plus `SCPH-18000`) — for the models FreeDVDBoot doesn't reach: `SCPH-9000x` because late-production units (Q3 2008+, per Wikipedia) ship a BIOS that blocks it, `SCPH-18000` because it predates FreeDVDBoot's supported range entirely. Uses the OpenTuna/Fortuna memory-card buffer-overflow exploit instead (prepared on a second console or a PC-based memory card adapter — cannot be bootstrapped from nothing on the stock unit itself, unlike the disc-based method), reaching the same FreeMcBoot 1.966 + OPL target. Includes an "Optional: ETH (network) mode" step (Slim generation has built-in Ethernet) alongside USB.
  - **`"Internal HDD (FreeHdBoot via Network Adaptor, no modchip)"` / `"FHDB (wLaunchELF 4.43x_isr)"`** (30 codes: `SCPH-30000`–`SCPH-50005`, the whole expansion-bay Fat generation) — writes a `wLaunchELF` KHN boot image straight to an IDE HDD from a PC (HDD RAW Copy Tool), no in-console exploit at all; the HDD's own EEPROM-triggered boot is the bootstrap. Covers all three requested loading media in one guide: HDD (primary, via the `+OPL` partition `FMCBinstaller.ELF` sets up), USB (alternate device mode), and ETH/network (OPL's SMB mode, using the same Network Adaptor already required for the HDD install).
  All three families sourced from israpps' own FreeMcBoot-Installer/FreeHdBoot documentation (same project already verified for `SCPH-70004`) plus cross-referenced OpenTuna/Fortuna project docs and OPL's official ETH-mode docs — not fabricated from a single source. See the Mind Tree vault project note/journal for the full research trail (sources, what's confirmed vs. inferred, and the ~15 still-unresolved regional model codes from the list gap-fill below).
  **Correction, same day, prompted by Andrei questioning the SCPH-9000x/Fat write-ups**: the official FreeDVDBoot project (`github.com/CTurt/FreeDVDBoot`, `freedvdboot.com`) has much broader confirmed compatibility than the OpenTuna/FreeHdBoot write-ups above assumed. Added: a `"Memory Card (softmod, no disc-swap, no donor card)"` combo (same content/steps as the `SCPH-70000x`–`79000x` guide) on all 6 `SCPH-9000x` codes — FreeDVDBoot's own notes call this generation "100% Verified" and explicitly "bypasses the infamous 90006/90001 FMCB BIOS blockade", i.e. the exact blockade the OpenTuna guide was written around; this is now the simpler PRIMARY method there, with the existing OpenTuna combo kept as a documented fallback (its intro was patched with a note pointing to this correction). Also added a `"Memory Card (softmod via FreeDVDBoot, no Network Adaptor/HDD needed)"` combo on the 3 Fat/Phat codes FreeDVDBoot's compatibility notes confirm BY EXACT DVD-PLAYER-FIRMWARE MATCH (not by model code alone): `SCPH-30000` (v2.10, ROMGEN `0160JC20010427`), `SCPH-30001R` (v2.10, ROMGEN `0160AC20010427`), `SCPH-30004R` (v2.10, ROMGEN `0160EC20011004`) — a simpler alternative to FreeHdBoot for those exact firmware versions, cross-referencing the Triangle-button DVD Player version check. The other 27 Fat codes' FreeHdBoot intro got a short note (prepended, not rewritten) explaining that FreeDVDBoot compatibility is firmware-version-dependent, not model-code-dependent, so a given unit MIGHT also work with it despite not being individually confirmed — FreeHdBoot itself doesn't depend on DVD Player firmware at all, so it stays the guaranteed method for those.
    - consoles.js — GET `/api/consoles?lang=` — serves console encyclopedia data per language from `consoles_translations`. `PUT /:id` (2026-09-06, new — `authRequired`+`adminOnly`) updates one console's EN row: validates `name`/`manufacturer` are present, whitelists+coerces `generation`/`release` (numbers), `models`/`advantages`/`disadvantages` (arrays, length/size-capped), `image` (string), and the six spec-group objects (`cpu`/`gpu`/`memory`/`storage`/`output_video`/`technologies`, passed through loosely — admin-trusted, same trust level as `content_html` elsewhere), then **merges onto the existing row** (`{...existing, ...whitelisted}`) rather than replacing it outright, so fields the edit form doesn't manage yet (e.g. the variable-shaped `dimensions` block some consoles have) survive untouched. Update-only — 404s if the id doesn't already exist, since a new id would have no matching static `consoles/<id>.html` page to ever display it on (deliberately no "add console" here, unlike the model-list "add" ask elsewhere in this task). Only the `en` row is editable from the site for now — see the `consoles_translations` architecture note below. **(2026-09-08 bugfix)** `models` was being sanitized as `body.models.map(m => String(m)...)` — since each model is `{name, year}`, that stringified every entry to the literal text `"[object Object]"` on any save through the edit form (found while working out the exact PUT payload shape for the Android app port, not by a reported symptom — unclear if any real console had been saved through the form since 2026-09-06 to actually hit this). Fixed to map each entry to `{name: string, year: number}` (filtered to drop nameless rows) instead of stringifying the whole object.
    - contact.js — contact form submission: honeypot + Turnstile CAPTCHA bot checks + sends email via Resend
    - courses.js — course/module/lesson/quiz/reactions/comments endpoints; also self-creates `lesson_reactions` table on load. **Admin lesson editing (2026-09-06, Phase 2 of the same admin-editing task as Evolution's console pages)**: `PUT /lessons/:id` (`authRequired`+`adminOnly`) updates `lessons.title`/`content_html` directly — these columns ARE the EN canonical row (mirrors `console_mod_tutorials` vs. its translations table; `lesson_translations` holds every other language, untouched here). `PUT /lessons/:id/quiz` (same guard) replaces the ENTIRE `quiz_questions` set for that lesson in one call — delete-then-insert inside a `BEGIN`/`COMMIT`/`ROLLBACK` transaction (`pool.connect()`, same pattern as `auth.js`'s account-deletion route) rather than per-question CRUD, so ids get reassigned on every save (not stable across edits — same tradeoff as the mod-tutorial `steps` array editor). Malformed question rows (empty question, <2 options, out-of-range `correct_option`) are silently skipped rather than failing the whole save, so one half-filled draft row doesn't cost the admin every other question they wrote.
    - dm.js — direct messages: conversation list, message thread, send DM (`POST /send` rejects with 403 while the sender is muted). Attachment validation (`attachment_type` `'image'|'voice'|'sticker'`, key must start with `chat/{images,voice,stickers}/{userId}/`) — Android bypasses this route entirely for actual sends (writes straight to Supabase via `SupabaseChatService.sendDm`, see the app repo), this exists for parity/any future website use of the route. Since 2026-07-25 (website chat-UI port, matching parity fields Android already writes directly to Supabase): `POST /send` accepts `reply_to` (validated to belong to the same conversation before insert); `GET /messages/:partnerId` returns `reply_to`/`edited_at` per message plus an aggregated `reactions: [{emoji, count, mine}]` array (one query against `message_reactions WHERE scope='dm'` for all message ids in the page); `PATCH /:id` edits own message text (sets `edited_at`); `DELETE /:id` deletes own message (no R2 cleanup, matches Android's own tradeoff); `POST /:id/react` / `DELETE /:id/react` add/remove a reaction row (`message_reactions`, `ON CONFLICT DO NOTHING` on add) — all four new endpoints mirror `ChatViewModel.kt`'s `commitEdit`/`deleteMessage`/`toggleReaction` semantics exactly. `reply_to`/`edited_at` columns and the `message_reactions` table were already live on Supabase (added by the Android app's direct-PostgREST development) and are now backfilled into `db.js`'s migration array too. `attachmentPayload()` also now returns the raw `key` alongside `url` (harmless — already derivable from the url path) so the client can Forward a message without re-uploading; `POST /send`'s attachment-key ownership check now also accepts a key that isn't the caller's own (`chat/{folder}/{callerId}/...`) as long as it already belongs to a real message the caller was sender or receiver of (dm) or any general-chat message — this is what makes Forward work, mirroring `forwardMessage()` in `ChatViewModel.kt` which reuses the same storage key without re-uploading (DMs only, never into general chat, same restriction as the app). **Real bug found via live Playwright browser testing (2026-07-25) and fixed**: `reply_to` is a `BIGINT` column, so `pg` serializes it as a *string* in query results, while `id` (SERIAL/int4) comes back as a plain number — `reply_to: row.reply_to` was silently breaking every reply-quote lookup client-side (`Map.get(number)` never matches a string key). Both `GET /messages/:partnerId` and `POST /send`'s response now do `row.reply_to ? parseInt(row.reply_to) : null` before returning it.
    - stickers.js — `/api/stickers`: metadata CRUD for the synced sticker library (`user_stickers` table, already existed for the Android app's direct-Supabase sticker feature but had no Express route until 2026-07-25). `GET /` lists the caller's own stickers newest-first; `POST /` validates `storageKey` starts with `chat/stickers/{userId}/` from a presigned upload; `DELETE /:id` is owner-only. Sticker bytes go straight to object storage via `POST /api/uploads/presign` (`kind: 'sticker'`), never through this route — same pattern as community-photos.js.
    - ebay.js — eBay OAuth connect/callback/status/disconnect, listings import, and account-deletion webhook verification
    - favorites.js — list/check/toggle favorite consoles for the logged-in user
    - forum-liked.js — GET `/api/forum/liked` — threads the user has upvoted (dashboard widget)
    - forum-my-posts.js — GET `/api/forum/my-posts` — threads created by the logged-in user (dashboard widget)
    - forum.js — forum thread list, thread detail, create, reply (optionally quoting a specific earlier reply via `reply_to_id`), upvote (notifies the thread/reply author via `createNotification(..., 'upvote', ...)`), mark-a-reply-as-solution (`POST .../:id/solve`, thread-author-only), `GET /search?q=` cross-console title/body search backing the global search bar; thread/reply creation rejects with 403 while the poster is muted; view counting already existed (`forum_threads.views`, incremented on every `GET /:console/threads/:id`) but wasn't surfaced in the UI until the Reddit-style redesign — `GET /:console/threads` (list) now also returns `LEFT(t.body, 200) AS body_snippet` for the card preview text. Threads can now optionally carry a single photo (Reddit-style image post) — `forum_threads.image_key` (nullable, added 2026-07-23), set on `POST /:console/threads` from an `image_key` the client got via `POST /api/uploads/presign` (`kind: 'forum'`), validated to start with `forum/image/${req.user.id}/`; both the list and detail GETs resolve it to `image_url` via `publicUrlForKey()`. Replies don't carry images — scoped to thread-level only.
    - friends.js — send/accept/reject friend requests, list friends, check friendship status
    - google-auth.js — Google OAuth2 via Passport: login/register/link/unlink, custom state store (no express-session); redirects to `login.html?error=account_banned&message=...` if the account is actively banned (`isActivelyBanned()`, same helper `routes/auth.js`'s `/login` uses — was raw `user.is_banned` before 2026-09-01, so an expired temp ban never got auto-lifted here). The `?state=platform=android` mobile flag (checked by the success path to redirect to `cnote://auth/callback?token=...` instead) is now ALSO checked on the ban path (`cnote://auth/callback?error=account_banned&message=...`) — previously it wasn't, so a banned account signing in from the app's Custom Tab got stranded on this site's own login.html instead of ever returning to the app (see `MainActivity.kt`'s `LaunchedEffect(intent)` on the app repo)
    - marketplace.js — marketplace listings CRUD: search, filter, sort, pagination; ties into OLX/eBay sync; notifies a listing's owner on favorite (`listing_interest`) and notifies everyone who favorited a listing when it's marked sold (`listing_sold` fan-out via `notifyListingSold()`); listing creation rejects with 403 while the poster is muted; every listing-returning query also derives `seller_is_official` (`users.role = 'admin'`) for the "Restored by CNote" badge — no dedicated column, purely computed at read time. `listings.images` used to store raw base64 data URLs directly (client-side canvas-resized then embedded whole in the JSON body — up to 200KB per image!); since 2026-07-23 it stores object-storage KEYS uploaded via `POST /api/uploads/presign` (`kind: 'listing'`), resolved to real public URLs at every read via the local `resolveImages()` helper (same key→URL indirection chat.js/dm.js use for `attachment_key`, via `publicUrlForKey()`) — old rows with full base64/external URLs still render fine since `resolveImages()` only rewrites strings matching the `marketplace/listing/` key prefix and passes everything else through unchanged
    - marketplace-reviews.js — seller reviews (`GET/POST /api/marketplace/sellers/:sellerId/reviews`+`/review`), mounted alongside marketplace.js; a review is tied to the seller (user), not a listing, so it survives the listing being sold/deleted — one upsertable review per (reviewer, seller) pair, mirrors `ratings.js`'s console-rating shape plus a `comment` field
    - orders.js — `/api/orders` (2026-08-21, new feature: no-payment checkout for marketplace listings). `POST /` (buyer, `authRequired`) — creates an order for a listing (`delivery_method: 'address'|'easybox'` + Sameday shipping fields: recipient name/phone, county/city/address for `address`, `easybox_name` for `easybox`, optional `notes`); rejects buying your own listing or an already-sold one; snapshots `listing_title`/`product_price` at order time (survives the listing later being edited/deleted); computes `shipping_price` from a flat `SHIPPING_PRICE` map (`address: 25`, `easybox: 20` RON, hardcoded — no live Sameday API integration) + `total_price`; on success immediately sets the listing `sold = TRUE, status = 'sold'` (a listing is one physical item, can't be bought twice — seller can revert via the existing `PATCH listings/:id/status` toggle if the order falls through). `GET /mine` (seller, `authRequired`) — the seller's own processing queue, joined with buyer username, `new` status floated to the top. `PATCH /:id/status` (seller only, `authRequired`) — toggles `new`/`shipped` (+ `shipped_at`). No payment gateway anywhere — payment is arranged directly between buyer and seller off-platform (Sameday COD / bank transfer), the site only collects shipping data and shows the fixed total. See `db.js`'s `orders` table, `community.js`'s buy modal, and `home.js`'s "My Orders" panel.
    - notifications.js — in-app notifications for forum replies, DMs, listing activity, etc.
    - ratings.js — console rating system: rate 1-5, view averages, see own ratings
    - repair.js — submit repair requests, view own/all requests, admin reply/status update; per-console `CONSOLE_SYMPTOMS` lists (must mirror `frontend/js/pages/community.js`'s `SYMPTOMS_BY_CONSOLE` word-for-word — validated server-side against it) include a `'Cosmetic restoration / upgrade'` option alongside fault symptoms, multi-selectable like the rest
    - reports.js — DSA Article 16 notice-and-action: submit content report, list own reports; content types: `forum_thread`/`forum_reply`/`direct_message`/`listing`/`user_profile`/`community_photo`; admin routes (`GET/PATCH /admin*`, gated by `adminOnly`) enrich each report with the reported content's `author_id` and expose moderation actions: `POST /admin/:id/ban-author`, `POST /admin/:id/unban-author`, `POST /admin/:id/mute-author` (`{hours}`, default 72), `DELETE /admin/:id/content` (deletes the reported listing/thread/reply/DM/community_photo; not available for `user_profile` reports — use ban instead). **`pushBanNotice(authorId, action)` (2026-09-01, new)** — fire-and-forget data-only FCM push (`sendPushToUser`, `type: 'account_banned'`) after `ban-author` (and reused by `admin-stats.js`'s direct `/users/:id/ban`) so a banned user's app logs itself out right away instead of only on its next 401 (see `MyFirebaseMessagingService.kt` on the app repo). Exported alongside `describeAction`/`sendOutcomeEmail`/`getUserEmail`.
    - reset-progress.js — POST `/reset-progress` — wipes a user's lessons/achievements/visits/favorites/XP, recomputes achievements
    - sessions.js — session management + Server-Sent Events stream for real-time session-termination notices
    - uploads.js — presigned PUT URL issuance for chat image/voice/sticker/community-gallery/tutorial/marketplace-listing/forum-post/article-cover/console-image attachments (`kind: 'image'|'voice'|'sticker'|'gallery'|'tutorial'|'listing'|'forum'|'article'|'console'`; file bytes never touch this server); `kind: 'tutorial'`/`kind: 'article'`/`kind: 'console'` additionally require `req.user.role === 'admin'` (console-model.html's write feature — see console-tutorials.js —, articles.js's cover image upload, and console-detail.js's admin edit modal, respectively); `kind: 'listing'` → key namespace `marketplace/listing/`, `kind: 'forum'` → key namespace `forum/image/`, `kind: 'article'` → key namespace `articles/image/`, `kind: 'console'` → key namespace `consoles/image/` (2026-09-06, new), `kind: 'sticker'` → key namespace `chat/stickers/{userId}/` (reuses the `image` content-type/size limits — see `IMAGE_TYPES`)
    - users.js — public user profiles, user search, console list loading (cached), owned-console management; `GET /users/:username` includes a real XP-based `level` object (via `getLevelFromXP`, same shape as `/api/me/level`), `null` when `show_stats` is off — no more client-side heuristic guess
    - workshop.js — admin-only hardware-diagnostics tool (boards/components/pins/journal); uses service-role Supabase client (RLS deny-by-default)
  - **scripts/**
    - check-dependencies.js — pre-deploy check that every `require()`'d package is declared in `package.json`
    - insert-translations.js — one-off script seeding French (and other) lesson translations directly into the DB
  - **services/**
    - email.js — Resend-based email service: verification, password reset, 2FA codes, contact form, branded HTML templates
    - firebaseAdmin.js — Firebase Admin SDK wrapper sending FCM push (currently DM push); auto-prunes dead tokens
    - marketplace-sync.js — picks the right `MarketplaceProvider` by name and drives listing sync
  - **test/**
    - **helpers/**
      - mockDb.js — `installMockDb()`: pre-populates `require.cache` for `../db` with a fake pool before any route/middleware file is required, so integration tests never trigger db.js's real Postgres connection (which calls `process.exit(1)` on failure — fatal in CI, where no DB is reachable)
    - authMiddleware.test.js — integration tests for `middleware/auth.js`'s `authRequired`/`authOptional` against a real Express app + mocked pool (JWT accepted/rejected, session-token fallback, anonymous pass-through)
    - device.test.js — unit tests for `utils/device.js` (browser/OS/device-type parsing, IP fallback order)
    - disposableEmail.test.js — unit tests for `utils/disposableEmail.js` (`isDisposableEmail()` coverage)
    - forumSearch.test.js — integration tests for `GET /api/forum/search` (routes/forum.js) against a real Express app + mocked pool
    - gamification.test.js — unit tests for `utils/gamification.js` (level curve, XP action shapes, achievement thresholds)
    - languages.test.js — unit tests for `utils/languages.js` (ALLOWED_LANGS/DEFAULT_LANG invariants)
    - passwordPolicy.test.js — unit tests for `utils/passwordPolicy.js` (`validatePassword()` rule coverage)
    - turnstile.test.js — unit tests for `utils/turnstile.js` (dev-skip, prod-fail-closed, Cloudflare success/failure/network-error paths)
  - **utils/**
    - device.js — parses `User-Agent` header into device type/browser/OS + IP, for session/security logging
    - disposableEmail.js — `isDisposableEmail()`: static blocklist of known temp-mail/throwaway domains, checked at registration
    - gamification.js — single source of truth for XP actions, levels, and achievement definitions; `awardXP()` lives here
    - languages.js — `ALLOWED_LANGS`/`DEFAULT_LANG` constants shared across routes needing language validation
    - moderation.js — `isMuted(user)`: checks `users.muted_until` against now; used as a posting guard in forum.js/marketplace.js/dm.js/chat.js. `mutedUntilMessage(mutedUntil)` (2026-09-01, nou) — the shared "You are restricted from posting until X" 403 text, all 5 call sites (was each building it inline with a raw `.toISOString()`, shown verbatim to the user — e.g. "2026-09-04T15:57:57.914Z" — now a readable `en-GB` date+time, explicit "UTC" since no per-user language is known this deep in a route handler)
    - objectStorage.js — generic S3-compatible client (Cloudflare R2 today, see `/infra/R2-SETUP.md`) for chat image/voice, community-gallery, console-tutorial, marketplace-listing, and forum-post-image attachments, server-side only; `buildAttachmentKey(userId, kind, extension, namespace='chat')` — e.g. gallery uploads pass `namespace: 'community'`; `uploadBuffer(key, buffer, contentType)` (added 2026-07-23) is for callers that already hold the final bytes server-side (e.g. auth.js's avatar route, which sharp-processes the image before storing — unlike everything else here, which is a client-presigned direct PUT)
    - passwordPolicy.js — single source of truth for password strength rules; `validatePassword()` used on all password-set paths
    - supabaseStorage.js — shared Supabase Storage client authenticated with the service-role key (avatar bucket), server-side only
    - turnstile.js — `verifyTurnstileToken()`: Cloudflare Turnstile (CAPTCHA) server-side verification, used by register + contact routes
  - .env — local environment values (gitignored, not committed)
  - .env.example — documented template of all backend env vars (DB, email, OAuth, storage, Turnstile CAPTCHA keys, etc.)
  - .gitignore — excludes `node_modules/`, `data/`, `.env`, local DB files from backend git tracking
  - db.js — Postgres pool setup (Supabase); `CREATE TABLE IF NOT EXISTS` (courses/modules/lessons/quiz_questions/lesson_translations/module_translations/user_lessons/user_course_progress/xp_transactions/user_achievements/content_reports/user_fcm_tokens/seller_reviews/community_photos/articles, among others) + idempotent `ALTER TABLE`/`CREATE INDEX` migration array run on boot (indexes cover forum/DM/notifications/marketplace/friends lookup columns; every statement runs inside its own swallow-all `try/catch`, so re-running is always safe, including `DROP CONSTRAINT`/`DROP POLICY ... IF EXISTS` + recreate pairs used to widen a CHECK or replace an RLS policy). Also adds `forum_threads.solved_reply_id` and `forum_replies.reply_to_id` (both `INTEGER REFERENCES forum_replies(id)`), `forum_threads.image_key` (nullable, single optional post photo), a partial index `idx_users_xp_desc` on `users(xp DESC) WHERE show_stats = true` for the leaderboard, moderation columns `users.is_banned`/`banned_reason`/`banned_at`/`muted_until`, and (2026-07-23) the `articles` table (slug/title/excerpt/content_html/cover_image_key/author_id/published/views) backing the new admin-written blog feature — see `routes/articles.js`. **(2026-07-24) Stickers**: widened `messages`/`direct_messages`' `attachment_type` CHECK to add `'sticker'`; new `user_stickers` table (`id`/`user_id`/`storage_key`/`created_at`, RLS: open read/delete, insert requires `storage_key LIKE 'chat/stickers/%'`) — the synced-across-devices sticker library the Android app's picker sheet reads/writes directly via Supabase (`SupabaseChatService.getMyStickers`/`addSticker`/`deleteSticker`); also **fixed a live RLS bug** found while adding this — `mobile_insert_dms`/`mobile_insert_messages` required `char_length(message) >= 1` unconditionally, which silently rejected every attachment-only send (`message = ""`) from the app's photo/voice/sticker attachment flows — replaced with `attachment_key IS NOT NULL OR char_length(message) BETWEEN 1 AND {2000,500}`. Applied live via the Supabase MCP `apply_migration` tool against project `gejxagsitdhkfmwfrmpn`, then mirrored into this array so a fresh bootstrap matches production. **(2026-07-25) Same bug, but on UPDATE**: `mobile_update_dms`/`mobile_update_messages` still required `char_length(message) BETWEEN 1 AND N` unconditionally — Postgres evaluates `WITH CHECK` against the *resulting* row regardless of which columns an `UPDATE` actually touches, so a read-receipt PATCH (`markDmsRead`, only sets `read = true`) on any attachment-only row was silently rejected, leaving the Android app's unread badge stuck forever on any conversation whose last message was a photo/voice/sticker. Fixed with the same `attachment_key IS NOT NULL OR char_length(message) BETWEEN 1 AND {2000,500}` `WITH CHECK`, applied live via Supabase MCP and mirrored here. **(2026-08-21) `orders` table** — no-payment marketplace checkout (see `routes/orders.js`): `listing_id`/`listing_title` (snapshot), `buyer_id`/`seller_id`, `delivery_method` (`address`/`easybox`), Sameday shipping fields (`recipient_name`/`recipient_phone`/`county`/`city`/`address`/`easybox_name`/`notes`), `product_price`/`shipping_price`/`total_price` (snapshotted at order time), `status` (`new`/`shipped`) + `shipped_at`. Applied live against the production Supabase DB by requiring `routes/orders.js` directly in the sandbox (confirmed via `information_schema.columns`/`pg_indexes` — table + both indexes present) — not just written and left for the next deploy.
  - package-lock.json — locked dependency tree for backend
  - package.json — backend deps/scripts (`start`, `dev`, `reset-db`, `precheck`, `test`)
  - README.md — backend setup instructions
  - server_check.js — minimal standalone Express+Helmet snippet to sanity-check CSP config changes in isolation
  - server.js — Express app entry point: middleware, CORS, CSP nonce injection (also injects `window.TURNSTILE_SITE_KEY`), mounts all routes, serves static frontend, 404 fallback (JSON for `/api/*`, branded `404.html` otherwise), Socket.io init. `GET /` redirects to `/html/pages/index.html` (an explicit filename, not just the directory — a bare `/html/pages/` redirect used to bypass the nonce-injection middleware since it only intercepts paths ending in `.html`, silently breaking the inline theme-restore script via CSP). Host-canonicalization middleware forces `www.consolenotebook.com` → apex domain (301) alongside the existing http→https upgrade — `www.*` used to serve the entire site as an independent, non-redirected duplicate. `LEGACY_REDIRECTS` map 301s old/guessed URLs (`/html/pages/terms.html` etc.) to their real current location, for URLs Search Console still shows as 404. A `setInterval` (every 6h, first run 5min after boot) calls `MarketplaceSyncService.syncAllUserListings(['olx'])` to re-sync connected marketplace accounts — `'ebay'` was removed from that call 2026-07-23 alongside the frontend deactivation, since the background job runs independently of the Settings UI and would otherwise keep trying (and, until the `expiresAt` Date bug fix, failing) to refresh tokens for any account connected before the deactivation. This is separate from — and doesn't affect — `routes/ebay.js`'s account-deletion webhook (`/api/ebay/account-deletion`), which is a mandatory eBay compliance endpoint eBay itself calls for any registered developer app regardless of integration status; disabling it risks eBay revoking API credentials, so it keeps running (the `eBay account deletion processed: ...` log lines are eBay notifying about *other* eBay users' account deletions, unrelated to CNote's own users). CSP `connectSrc` computes TWO object-storage entries now (2026-07-23 fix): `objectStorageOrigin` (from `OBJECT_STORAGE_PUBLIC_URL`, unchanged — lets `<img>`/`<audio>` fetch already-uploaded files) and the new `objectStorageUploadOrigins` (from `OBJECT_STORAGE_ENDPOINT` — the bare origin *and* a `https://*.<host>` wildcard). The direct-to-R2 browser PUT upload (`fetch(presign.uploadUrl, {method:'PUT', ...})`, used everywhere presign-then-PUT is used: listings, forum images, avatars-via-tutorial-photos, DM attachments, Community Photos, article covers) was being silently blocked by CSP the whole time it was missing — R2's virtual-hosted-style addressing (`FORCE_PATH_STYLE=false`) prefixes the bucket name onto the endpoint hostname for actual requests, so even the bare endpoint origin alone wouldn't have matched; the browser reports this as a bare "Failed to fetch" with no network request ever sent, no CORS error, and nothing in server logs — first diagnosed 2026-07-23 from Andrei's real browser DevTools console output after R2 was fully configured (CORS + Railway env vars both confirmed correct first).
- **infra/**
  - .env.example — template for MinIO root user/password on the self-hosted VPS (see README.md note below — not the active setup)
  - Caddyfile — reverse proxy config: TLS (Let's Encrypt) in front of the self-hosted MinIO object storage
  - docker-compose.yml — deploys MinIO + Caddy on a VPS for chat attachment storage (Supabase Storage free tier too small)
  - README.md — why/how of the self-hosted MinIO-on-VPS object-storage setup; now marked as a kept-for-later migration path, not the active one (see R2-SETUP.md)
  - R2-SETUP.md — **active setup**: step-by-step Cloudflare R2 bucket + scoped API token + CORS + Railway env var instructions for `OBJECT_STORAGE_*` (chat attachments, Community Photos, console tutorial/modding step photos). Chosen over self-hosted MinIO for zero server/DNS/Docker management and a 10GB free tier with no egress fees; `backend/utils/objectStorage.js` is unchanged either way (plain S3-compatible client, provider swap = env vars only)
- **frontend/**
  - **assets/**
    - **icons/** — favicon.ico (24×24 red notebook logo; also used as the navbar `.logo-icon`); apple-touch-icon.png (180×180), icon-192.png, icon-512.png (PWA manifest icons) are all upscaled from favicon.ico itself (nearest-neighbor, so notebook branding stays consistent everywhere — blocky/pixel-art look at large sizes since the source is only 24×24, but no other higher-res logo source exists in the repo)
    - **images/**
      - **consoles/** — ~104 files (PNG+WebP pairs), one pair per console, filename = console slug; referenced by encyclopedia/comparison pages
      - **wallpapers/** — 8 full-page background images, one per major page (home, community, comparatie, evolutie, help, index, invata, login)
      - og-image.png — 1200×630 social-share preview image (notebook favicon upscaled onto the dark theme background `#1B1714`, lossless PNG), referenced by `og:image` on index.html/home.html
    - **vendor/**
      - **katex/** — vendored KaTeX library (JS/CSS/fonts + auto-render contrib) for math rendering in course lessons
  - **css/**
    - **base/** — reset.css (global resets + site-wide `:focus-visible` outline + `.skip-link` styling), typography.css, variables.css: type scale, CSS custom-property theme tokens
    - **components/** — reusable component styles (buttons, cards, forms, cookies banner, avatar cropper, quiz, report modal, search/profile dropdown, levels, index cards, date picker, sections, quick-start-guide); forms.css's bare `input[type=text/number/email/password]`/`textarea`/`select` rule is the only styling for any plain form field site-wide with no page-specific class (e.g. Settings/profil.html's username/email/password/bio/social-link fields) — 2026-07-25's "chat text bos style" commit accidentally stripped its `background`/`border`/focus `box-shadow` thinking it only affected chat (the DM composer actually has its own `.hub-dm-form__input` class, untouched by this rule), leaving those Settings fields with no visible background/border and no keyboard focus indicator (its `outline:none` beats reset.css's site-wide `:focus-visible` outline by specificity); reverted 2026-07-27, restoring `background: var(--card-bg)`/`border: 1px solid var(--border-color)` and the focus `border-color`/`background`/`box-shadow` ring. Matching revert in utilities/helpers.css below (`a/button/input:focus-visible` had lost its `outline: 2px solid var(--primary-color)` in the same commit).
    - **layout/** — footer.css, grid.css, hero.css, navbar.css: page-shell/structural layout styles. **(2026-09-02)** `navbar.css`'s `.navbar` (site-wide `position: fixed` navbar) gained `transform: translateZ(0)` (also folded into both `.navbar--hidden`/`.navbar.navbar--hidden` rules' own `translateY(-100%)`, since `transform` replaces wholesale rather than merging) — forces its own GPU compositor layer, a documented workaround for Android/Chromium mobile browsers corrupting/duplicating the paint of `position: fixed` elements. Prompted by a real duplicated/ghosted rendering screenshot on Samsung Internet in DeX mode; Samsung Developer Forums separately confirm a reproducible "fixed element + overflow sibling breaks viewport" bug on Samsung Internet 16-17.x that this site's fixed-navbar-plus-`overflow`-heavy layout matches. Deliberately `translateZ(0)`, not `will-change: transform` — the latter is documented to break `position: fixed` outright on some engines.
    - **pages/** — one stylesheet per page (auth-profile, community, comparatie, console-detail, contact, course, evolutie, help, home, index, leaderboard, lesson, notfound, stats, terms, user-profile, etc.); community-hub.css also carries the forum "solved" badge/highlight and reply-context styles (`.hub-form-textarea` min-height raised from 90px to 160px, resizable, with its own larger mobile min-height (140px) split out from the shared `.hub-form-input`/`.hub-form-select` 48px tap-target rule so the description box in the New/Edit listing modals doesn't get squashed back down on small screens); the forum thread cards and comments were redesigned Reddit-style (`.hub-thread-card`, `.hub-vote-pill`, `.hub-action-pill`, `.hub-comment`/`.hub-comment-thread`, `.hub-thread-card__image` for the optional post photo) — see `community.js`'s entry below for what replaced the old `.hub-thread-item`/`.hub-reply-card` markup; DM form gained `.hub-dm-form__icon-btn` (round +/🎤 buttons, `--recording` modifier pulses red while a voice message is being captured), `.hub-dm-pending` (removable image-attachment preview chip shown above the input before sending), and `.hub-dm-msg__image`/`.hub-dm-msg__audio` for rendering sent/received attachments inline in the message bubble — light-theme overrides added alongside the existing `.hub-dm-form__input` ones; `.hub-market-topbar` (the search input + Filters button row above the listings grid) was missing horizontal/top padding on desktop entirely — the mobile breakpoint override already had `padding: 10px 12px` (so it looked fine on mobile) but the base desktop rule had none, leaving the search bar and Filters button flush against the view's rounded edges instead of matching `.hub-view-header`'s/`.hub-market-grid`'s 24px side padding — fixed 2026-07-23 by adding `padding: 16px 24px 0` to the base rule; leaderboard.css and notfound.css are bundled into main.css like the rest (no separate `<link>`) — gold/silver/bronze podium styling for ranks 1-3 is shared via `nth-child` selectors across `.lb-row`/`.idx-leaderboard__row` (leaderboard.html, index.html, and home.html's Progress-panel preview all inherit it for free); **2026-07-25 — DM bubbles redesigned iMessage-style** (see `community.js`'s matching entry above for the full JS-side rewrite): `.hub-dm-msg--mine` is now a glossy `linear-gradient` built from `color-mix(in srgb, var(--accent-color) ...)` (auto-adapts per theme — near-white gloss on dark, near-black on light — no separate light-theme override needed); `.hub-dm-msg-row`/`--stacked`/`.hub-dm-msg--tail` handle iMessage-style consecutive-bubble corner-radius grouping; new: `.hub-dm-reply-quote*` (inline reply quote inside a bubble), `.hub-dm-reactions`/`.hub-dm-reaction-chip*` (tapback-style chips), `.hub-dm-msg--sticker`/`__sticker-img` (no bubble chrome, matches the app's WhatsApp-style stickers), `.hub-dm-voice*` (custom waveform player replacing `<audio controls>`), `.hub-dm-reply-banner*` (reply/edit banner above the compose form), `.hub-dm-sticker-panel*` (sticker picker grid), `.hub-ctx-scrim`/`.hub-ctx-menu*` (shared floating context-menu chrome for both the message and conversation right-click menus), `.hub-img-viewer*` (full-screen zoom viewer — deliberately a plain `position:fixed` overlay, not a dialog, so edge-to-edge top/bottom gradient fades just work), `.hub-forward-list`/`.hub-forward-conv` (forward picker, reuses `.hub-modal-overlay`/`.hub-modal`); **(2026-09-01)** new `.hub-dm-conv--general` (+ its `:hover`) tints the pinned "General" row `community.js`'s chat/DM merge adds atop the conversation list — accent-tinted background/border, no separate light-theme override needed since it's built from `rgba(var(--accent-rgb))`; **(2026-09-02)** new `.hub-dm-msg__sender` (name label above a "theirs" bubble group in general chat, see `chat.js`) and `#view-chat .hub-dm-back-btn` (forces general chat's back button visible on desktop too, unlike a DM thread's own mobile-only `.hub-dm-back-btn`) added, and the entire old "CHAT REDESIGN" section (`.chat-hdr*`/`.chat-msg*`/`.chat-box`/`.chat-input-pill*`/`.chat-empty-state*`/`.chat-typing*`, plus every `.hub-view--chat .chat-*`/`.community-container` override scattered across base/mobile/light-theme blocks) was deleted — general chat's message/header/composer design was unified with a DM thread's instead, and none of those classes are emitted by `chat.js`/`community.html` any more (confirmed: the community-welcome-page's similarly-named `cl-chat-msg*` is an unrelated, separate namespace, untouched). `.chat-date-divider` is the one piece of the old design kept, still used by `chat.js`'s `appendMessage()`. **(2026-09-02, later same day)** `#view-chat`/`.hub-view--chat` and its now-pointless always-visible-back-button override removed entirely (`#view-chat` no longer exists — general chat moved into `#view-dm`, see `community.js`'s `openGeneralChat()`); new `#dm-thread[hidden], #chat-thread[hidden] { display: none !important; }` added right after `.hub-dm-thread`'s base rule — needed because `.hub-dm-thread`'s own `display: flex` would otherwise beat the browser's default `[hidden]` rule (equal specificity, author styles win over user-agent ones), so toggling the `hidden` attribute between DM's and general chat's thread panes wouldn't actually hide the losing one without this. **(2026-09-02, later same day — Samsung DeX rendering research)** two more prongs of the same fix as `navbar.css`'s (see `layout/` entry above): `.community-page--hub`'s background lost its `fixed` attachment (kept `center/cover no-repeat` only) — well-documented as unreliable on Android/Chromium mobile browsers, and the page never actually scrolls anyway (`overflow: hidden`) so it bought nothing; `body.community-navbar-page`/`.community-page--hub`'s height cascade gained a third, winning `height: 100svh` after the existing `100vh`/`100dvh` fallbacks — `100dvh` recalculates (and repaints) as browser chrome shows/hides, which lines up with the still-unconfirmed "whole page scrolls as one ~90-100px unit" DeX report from 2026-09-01; `100svh` locks to the smallest possible viewport and never recalculates. `svh`/`dvh` share the same browser-support cutoff (Samsung Internet 21+), so this is a safe drop-in — anything that already understood the existing `dvh` fallback understands `svh` too. **(2026-09-06)** `lesson.css` gained `.lesson-edit-*`/`#lesson-edit-status` — the admin full-content lesson editor (`lesson.js`'s `openLessonEditor()`: title/content/quiz-question cards with add/remove options), its own copy of the same `.article-modal*`/`.console-edit-*` overlay pattern (lesson.html doesn't link either of those stylesheets).
      - console-detail.css — per-console detail page (`consoles/*.html`) stylesheet, imported globally via `main.css` (unlike console-care.css, which is opt-in per page) — hero/specs/history/video-montage sections, `.dim-table` (dimensions table, referenced but not reused by ps3-cfw-compatibility.html, see console-care.css's note below), and (2026-09-06, new) `.console-edit-*`/`#console-edit-status` — the admin full-content edit modal (`console-detail.js`'s `openConsoleEditor()`), styled as its own copy of articles.css's `.article-modal*` overlay pattern since console pages don't link articles.css.
      - console-care.css — console-care.html's own stylesheet, linked directly (not bundled into main.css, same as lesson.css) since it's opt-in for one page: hero, the `.model-plate` hardware-ID-sticker component (signature visual — styled like the real model-number label printed on a console's underside, deliberately theme-invariant/always a light plate since it represents a physical object), directory search/tabs/grid, `.care-topic` panel grid (replaced an earlier `.terms-page` sidebar-TOC layout that reused the legal-docs styling — rejected as looking wrong for a marketing/content page), `.tutorial-*`/`.tutorial-editor__*` (console-model.html's read view + admin editor), `.tutorial-selectors`/`.tutorial-select` (the modding-guide flash-type/firmware-version dropdowns), `.warning-block`/`.living-doc-note` (safety callout + "unverified placeholder" note, both new for ps3-cfw-compatibility.html — `.warning-block` mirrors `lesson.css`'s `.myth-block` box styling but with a "⚠ Warning" `::before` label instead of "Myth"), and `.compat-table*` (the reference-table styling, since no reusable `<table>` CSS existed anywhere this page already links — `console-detail.css`'s `.dim-table` lives in an unlinked stylesheet). **(2026-09-08)** `.model-plate__meta` (the console-name + note line on the directory grid's `.model-plate` cards, `model-directory.js`) gained a fixed 2-line clamp (`max-height: 2.8em`, `-webkit-line-clamp: 2`/`line-clamp: 2`, ellipsis) via `.model-plate:not(.model-plate--large) .model-plate__meta` — a long note was overflowing the card unevenly; the detail page's `.model-plate--large` (console-model.html, note text shown elsewhere, unclamped) is excluded.
      - articles.css — articles.html/article.html's own stylesheet, linked directly (not bundled into main.css, same pattern as console-care.css) plus also linked from home.html (for the dashboard's "Articles" preview section, which reuses `.article-card` as-is via `.articles-grid--preview`, a 3-column compact variant). Covers the list page (hero, `.articles-grid`/`.article-card`, load-more button), the detail page (`.article-view`, cover image, admin action row), and the shared write/edit modal (`.article-modal-overlay`/`.article-modal`, cover-image upload chip) used by both articles.js and article.js. Also defines `.container--narrow` (760px) for the detail page's reading-width column.
    - **utilities/** — animations.css, helpers.css, responsive.css: utility classes and breakpoint overrides
    - main.css — entry point that `@import`s all of the above in cascade order
  - **html/**
    - **components/**
      - footer.html — shared site footer markup, injected client-side into every page's `#footer-placeholder`
      - navbar.html — shared site navbar markup, injected client-side into every page's `#navbar-placeholder`
    - **js/**
      - components.js — fetches navbar.html/footer.html, sanitizes with DOMPurify, injects into placeholders; adds a skip-to-content link + tags the main content landmark for a11y; also lazy-loads Sentry with server-injected DSN
    - **pages/**
      - **consoles/** — 52 static per-console detail pages, one per console, filename = console slug, rendered via `console-detail.js` + `data-loader.js`; each now has a unique `<title>`/meta description/og:tags (name, manufacturer, release year, own image) generated from `consoles-en.json` — previously every one of the 52 shared the identical generic "Console Notebook" title and description
      - **help/** — 10 static help/FAQ sub-pages (achievements, community, console, account, forum, general, marketplace, friends, profile, repair)
      - **legal files/** — 24 files: community-rules/cookies/privacy/terms, each translated into 6 languages (default + de/en/es/fr/it suffixed variants)
      - 404.html — branded not-found page ("Cartridge Not Found" — retro-themed, floating controller icon, quick links to Encyclopedia/Learn/Compare/Leaderboard); served by `server.js`'s catch-all 404 fallback (HTML 404 status, CSP nonce injected)
      - article.html — dynamic single-article detail page, `?slug=<slug>` query param (same pattern as console-model.html), logic in `pages/article.js`. Shows cover image (if any)/title/author/date/views, then `content_html` rendered raw into a `.lsn-content` container (reuses `lesson.css`'s rich-text typography component instead of duplicating it). Admin (`AuthModule.getCurrentUser()?.role === 'admin'`) sees Edit/Delete buttons — Edit opens the same modal markup/classes as articles.html's create modal, prefilled, saving via `PUT /api/articles/:id`; Delete confirms via `utils/confirm-modal.js` then `DELETE /api/articles/:id` and redirects to articles.html. No canonical tag (dynamic per-slug page, same precedent as console-model.html); not in either sitemap.xml (individual articles aren't enumerated, only the articles.html list page is — same precedent as console-model.html vs. console-care.html/console-modding.html).
      - articles.html — public articles/blog list page (2026-07-23), linked from a new "Articles" section on home.html. Grid of `.article-card`s (cover/title/excerpt/author/date), paginated via a "Load more" button (`pages/articles.js`, `GET /api/articles`). Admin sees a "✎ Write article" button (hidden via the `hidden` attribute by default, shown by JS after `AuthModule.getCurrentUser()?.role === 'admin'` check) opening a create modal (title/excerpt/cover image upload via `POST /api/uploads/presign` `kind: 'article'`/content HTML textarea/published checkbox) that `POST`s to `/api/articles` and redirects to the new article's `article.html?slug=`. Listed in both sitemap.xml copies (daily changefreq, since new articles appear here). Links `css/pages/articles.css` directly (not bundled into main.css, same pattern as console-care.css) — also linked from home.html for the dashboard preview section's card styling.
      - community-welcome-page.html — logged-out landing page introducing the Community hub before login
      - community.html — main Community hub shell: forum, marketplace, repair wizard, DMs, Photos gallery (logic in `pages/community.js`). Sidebar section order (top to bottom): Marketplace (Toate/Console/Jocuri/Accesorii/Parts) → Messages (Direct messages) → Chat general (Community chat + Forum General + Community Photos, the last one moved in from its own former section) → Retro Gaming (`community_retro_gaming_heading`, renamed from "Coming soon" — still holds the locked "🎮 Community Games Chat" item with its own "Coming Soon" badge) → Console categories (per-console Request community/Request repair) → Repair (My Requests / Users Requests, admin-only). The mobile "More" dropdown (built by `community.js`'s `buildDropdown()`) derives its items from these sidebar sections, so it follows this same order automatically. Marketplace/"Toate" is now both the default landing view (see `handleHashNavigation()` in community.js) and the first sidebar section. **Chat/DM merge (2026-09-01)**: the separate "Messages" (Direct messages) and "Chat general" (Community chat) sidebar sections were folded into one "Chat" section with a single `data-view="dm"` item — general chat is no longer its own sidebar/bottom-nav destination, it's now a pinned row inside the DM conversation list (see `community.js`'s `loadConversations()`). The mobile bottom nav's "Chat" button (`data-mbn-view`) now points at `dm` instead of `chat` for the same reason. **(2026-09-02)** `#view-chat`'s own markup was rebuilt to match a DM thread's structure/classes (`.hub-dm-thread`/`.hub-dm-thread__header`/`.hub-dm-messages`/`.hub-dm-compose-dock`/`.hub-dm-form`) instead of its old bespoke `.chat-box`/`.chat-hdr`/`.chat-input-pill` markup — see `chat.js`'s matching entry for the message-bubble side of this and `community-hub.css`'s for what CSS it retired. **(2026-09-02, later same day)** `#view-chat` removed entirely (Andrei: it rendered oddly under the navbar, and on desktop opened full-page instead of beside the list like a real DM thread) — general chat now lives permanently inside `#view-dm` as `#chat-thread`, a static sibling of `#dm-list`/`#dm-thread` inside `.hub-dm-layout`, so on desktop it occupies the exact same two-pane "thread" slot a DM conversation does (list stays visible alongside it) and on mobile it fullscreens the same way a DM thread does — see `community.js`'s `openGeneralChat()`/`closeGeneralChat()`.
      - comparatie.html — hardware side-by-side console comparison tool page; supports `?a=<id>&b=<id>` query params (not `#hash` — Google doesn't index fragment identifiers) so any pair is a shareable/indexable URL; `og:title`/`og:description`/`og:url`/`og:image` meta stubs present for `comparatie.js` to rewrite per-pair
      - console-care.html — static, public "Console Care & Cleaning Guide" page (no login required), own design (`console-care.css`, not the legal-docs `.terms-page` look). Order: hero (with 3 decorative `.model-plate` mockups) → a short "Quick Tips" checklist (`.care-tips`, 7 one-liners — moved to right after the hero, above the directory, since a quick-scan list reads better as an intro than a final section) → "Identify Your Model" directory (`#care-directory-grid`, searchable/filterable by manufacturer, populated via the shared `initModelDirectory()` in `modules/model-directory.js` from real researched PlayStation/Xbox/Nintendo hardware model numbers, 194 entries incl. individual regional/revision variants, e.g. PS2 `SCPH-50004`, PS2 `SCPH-30000R` (a distinct board revision, not just a region code — reversed power/reset switch wiring), PS3 `CECHC04` — each plate is a clickable `<a>` to `console-model.html?code=<code>`) → a pill chip-nav → maintenance topics as `.care-topic` panel cards (vents/dust, cartridge contacts, optical drive/laser lens care, controller stick drift, safe opening, storage/transport, glossary, CTA into the Community repair-request wizard `community.html#repair-requests`) using `lesson.css`'s callout components (`think-block`/`myth-block`/`glossary-block`/`cta-block`). Static Article+BreadcrumbList JSON-LD in `<head>`; all body copy is `data-i18n`'d (`care_*` keys, all 6 languages) — section headings are plain topic labels, not numbered, since these are independent reference topics rather than a sequence. Linked from a 4th card on invata.html's course grid and from a new FAQ entry (`help_con_q8`/`a8`) on help-console.html; listed in both sitemap.xml copies. Verified with a real headless-browser render (default/dark/light themes) before commit — caught and fixed a wrong `../../js/components.js` script path (should be `../js/components.js`, one level up from `html/pages/`) that had silently broken navbar/footer injection since the page was first created.
      - console-modding.html — static, public "Console Modding Guide" page, same page pattern and stylesheet as console-care.html (`console-care.css` is reused as-is, not duplicated — its `.care-*` class names are the site's general "reference guide page" component system, not cleaning-specific). Order: hero → "Quick Tips" (`mod_tip_1..7`) → "Find Your Model" directory (same `#care-directory-grid` markup, same shared `initModelDirectory()` from `modules/model-directory.js`, same `MODELS` data and same clickable plates linking to `console-model.html?code=<code>` — clicking a model here vs. from console-care.html lands on the exact same page, just a visitor coming from the modding guide is more likely to open its "Modding Guide" section rather than "Disassembly Tutorial"; a `.cta-block` right above the grid links to `ps3-cfw-compatibility.html`) → chip-nav → topic panels (softmods vs hardmods, tools needed, backup-first, warranty & console bans, staying legal, glossary, get-help CTA into `community-welcome-page.html`). One CTA links to the Starter Guide's `course.html?slug=starter-guide` (module 7, "Understanding Console Modding," covers the concepts/risks in depth). All copy is `data-i18n`'d (`mod_*` keys, all 6 languages). Listed in both sitemap.xml copies; linked from invata.html's 3rd course card (replacing its old "Coming Soon" badge).
      - ps3-cfw-compatibility.html — static, public "PS3 CFW Compatibility Chart" page (2026-08-07), same `console-care.css` page pattern as console-modding.html/console-care.html (no `lesson.css`, doesn't need its callout components — uses new `.warning-block`/`.living-doc-note`/`.compat-table*` classes added to `console-care.css` instead). Three tables built as **pure structure, deliberately not populated from external research**: Table 1 (PS3 model compatibility, grouped Fat/Slim/Super Slim, one `<table class="compat-table">` per chassis group, rows = every model code already in `console-models.js`), Table 2 (firmware compatibility, rows = OFW brackets `3.55`/`3.56–4.80`/`4.81–4.89`/`4.90`/`4.91`/`4.92`/`4.93`), and a small "Recommended Path" model+firmware lookup table. Every cell reads the literal `data-i18n="mod_compat_not_tested"` ("Not yet tested / documented") placeholder **except** the one row the site owner has personally tested — PS3 `CECHJ` (CECH-J03) / NOR / OFW 4.93 — which links to `console-model.html?code=CECHJ&from=modding`. Intentionally NOT sourced from psdevwiki/psx-place/GitHub — rows get filled in later, by hand, only as each combination is personally verified; no "confirm manually"-style hedge language anywhere, `mod_compat_not_tested` is the only unverified-state string used. Cross-linked from console-modding.html's new CTA. Not yet in sitemap.xml (mirrors console-model.html's noindex-until-real-content precedent, even though this page itself is static — most of its content is still placeholder).
      - console-model.html — dynamic per-model detail page, `?code=<code>` query param (e.g. `?code=SCPH-50004`), no static per-model files (same query-string pattern as comparatie.html). Reads `frontend/js/data/console-models.js` via `console-model.js`, shows the model's plate + manufacturer/console/note, plus TWO independent write-up areas: "Disassembly Tutorial" (linked from console-care.html, unchanged — single row per model, `GET/PUT/DELETE /api/console-tutorials/:code`) and "Modding Guide" (linked from console-modding.html) — the latter now supports **multiple independent write-ups per model**, one per `(flash_type, firmware_version)` combination (e.g. PS3 `CECHJ`'s `NOR`/`4.93` CFW walkthrough), picked via two `<select>`s (`#mod-flash-select`/`#mod-version-select`, `.tutorial-selectors`/`.tutorial-select` in `console-care.css`). The selector's option lists are the UNION of `console-models.js`'s `MOD_OPTIONS` (a static per-`console` menu — just a pick-list, no compatibility claim — currently defined for `'PS3'`/`'PS3 Slim'`/`'PS3 Super Slim'`) and whatever real combos already exist, so the dropdowns are visible and usable on a PS3 model page from the very first visit, well before any write-up exists for most combinations — picking an unwritten combo shows `#mod-combo-missing` ("not available yet"), distinct from the all-zero `#mod-coming-soon` state (only shown when a model has neither a `MOD_OPTIONS` entry nor any real combo, i.e. non-PS3 models today). Admin-only (`AuthModule.getCurrentUser()?.role === 'admin'`) sees "Edit tutorial" (current combo, when one exists) and "+ Add tutorial for a new flash type / firmware version" (always available, pre-filled with whatever combo is currently browsed) buttons — the latter's editor shows `<select>`s (drawn from the same option list) when one exists, else falls back to free-text inputs. `<meta name="robots" content="noindex, follow">` until real tutorial content exists. Falls back to a "model not found" message + link back to the directory if `?code=` doesn't match anything. PS3 `CECHJ`/`NOR`/`4.93` has real content live in `console_mod_tutorials` since 2026-08-07 (the site owner's personally-tested CFW write-up, 13 steps covering setup through Verified-On/See-Also) — seeded directly via a one-off local script against production Postgres (not through the admin UI), since it's the same upsert path `PUT /:code/mod/:flashType/:version` uses.
      - course.html — single course overview page (modules/lessons list), driven by `pages/course.js`
      - evolutie.html — console evolution timeline / encyclopedia grid page
      - help.html — Help & Support page: FAQ accordion, category filter, search; contact form includes a Turnstile CAPTCHA widget
      - home.html — logged-in home/dashboard page (quick-start timeline, stats, feed); self-referencing canonical (fixed from wrongly pointing at `/`); `<title>` leads with "Console Notebook" so it doesn't get truncated to "Cnote Bak…" in narrow browser tabs; the Progress panel now embeds a "Top Contributors" leaderboard preview (below the All Levels list) with a link to the full leaderboard.html — previously the leaderboard was only reachable from the mobile "more" dropdown, invisible on desktop; no longer loads `index-cards.js` (was dead code here — targeted DOM IDs that don't exist on this page); the Collection panel's "My Listings" section (`#my-listings-section`) is no longer `hidden` when a user has zero listings — it now always renders, showing a `.dash-empty-state` placeholder when empty, with a "+ Add listing" link (`.panel-section-title__link`) in the section header pointing to `community.html#marketplace/new` (a deep link `community.js`'s `handleHashNavigation()` recognizes to jump straight to Marketplace and open the "New listing" modal); new "Articles" section shows the 3 latest posts from the new blog feature as `.article-card`s (via `css/pages/articles.css`, linked here too). Home panel (`data-panel="home"`) section order (2026-07-23 reorder, requested explicitly): hero → **Articles** (`#home-articles-preview`, first content section) → Continue-course card + Quick Start Guide → **Trending (forum) + Marketplace** two-col (`#home-trending-preview` promoted up from its old pairing with Friends; `#home-marketplace-preview` is new, newest-listings via `GET /api/marketplace/listings?limit=4`, reuses `.home-listing-card` markup/CSS from the "My Listings" widen instead of a bespoke card) → Stats → Activity + Achievements (unchanged pairing) → **Friends + Your Collection** two-col (new pairing — Friends kept its `#home-friends-home-preview` content, Collection's standalone section was folded in as this row's side column instead) → Ratings preview. Every preview section's `<h2>` now carries a `.panel-section-title__link` "See all" (`home_see_all` i18n key, reused by Trending/Marketplace/Activity/Friends/Collection/Ratings — Articles keeps its own pre-existing `home_articles_see_all` key, and Achievements deliberately has none here since it already had a richer dynamic one, see `home.js`) pointing at: `community.html#forum/general` (Trending), `community.html#marketplace` (Marketplace), `statistici.html` (Activity), `#friends`/`#collection` (Friends/Collection — real in-page panels, same hash-routed `data-panel` system as the sidebar), `evolutie.html` (Ratings). The course-continue card (2026-07-23) is now wrapped in its own `<section class="dashboard-section continue-section">` with a `panel-section-title` heading (`home_continue_section_title`, "Continue Learning") — it previously sat in a bare `<div>` with no heading at all, so it had zero top margin and looked jammed directly under Articles with no visual separation (every other section's spacing comes from its `panel-section-title`'s `margin: 12px 0`, not from any base `.dashboard-section` rule, since there isn't one). `#quick-start-guide`'s own `margin-top` (in `css/components/quick-start-guide.css`) bumped from 10px to 20px for the same reason — it already had its own internal title (`.qsg-title`), just too little breathing room above it. **(2026-08-21) New "My Orders" panel** (`data-panel="orders"`, own sidebar link + `#orders-new-badge` unread-style count) — the seller's no-payment-checkout queue, added right after the "Liked Posts" panel; `#orders-grid` renders `.order-card`s built by `home.js`'s `renderOrderCards()`. See `home.js`'s entry for the render/copy/status logic and `routes/orders.js` for the backend.
      - index.html — logged-out landing page (marketing/intro), thumbnail-to-featured console showcase; self-referencing canonical (fixed from wrongly pointing at `/`, which is just a redirect); `<title>` leads with "Console Notebook" for the same tab-truncation reason; below the notebook scene, a "Learn/Encyclopedia/Compare" highlights strip and a live "Top Contributors" leaderboard preview (top 5, `pages/index.js`) for social proof on the logged-out landing page
      - invata.html — "Learn" page: course catalog with per-course progress bars. Only "Console Starter Guide" (`starter-guide`, 7 modules/21 lessons in the `courses`/`modules`/`lessons` DB tables — module 7 "Understanding Console Modding" covers softmod/hardmod concepts, legality, safety and warranty) is actually live; "Console Engineering" is a real `courses` row too but has zero modules/lessons — shown as a "Coming Soon" card. The old "Console Modding" `courses` row (id 3) was deleted — modding isn't a structured course, it's a per-model tutorial guide like Console Care (see console-modding.html) — its card now links straight there instead of being "Coming Soon". The hero title/intro/stats (`learn_section_title`/`learn_section_intro`/`learn_body`/`learn_rec1_text` in `i18n.js`, all 6 languages) used to describe "Console Engineering" (8 modules, electricity/electronics/pixel-on-screen) as if it were the live course — fixed to describe the actual Starter Guide instead; don't let this drift again if Engineering gets real content later. Course grid's 3rd card links to `console-modding.html` (Modding Guide), 4th card links to `console-care.html` (Cleaning & Care Guide).
      - leaderboard.html — public XP leaderboard page (no login required), driven by `pages/leaderboard.js`; linked from the mobile-nav "more" dropdown on most pages and from the community-welcome-page's "Give Back" step (which previously promised a leaderboard that didn't exist)
      - lesson.html — single lesson viewer page (video/text/quiz), driven by `pages/lesson.js`
      - login.html — login page: server/local login, Google OAuth, 2FA, resend verification
      - profil.html — account Settings page: profile, privacy, security, notifications, appearance; admin-only tabs: Reports (moderation) and Analytics (growth/activity stats). Marketplace Connections row: both provider cards (`#olx-provider-card`, `#ebay-provider-card`) are now static "Coming soon" (no button ids) — eBay was fully functional until deactivated 2026-07-23 (UI-only; backend untouched, see `profile.js`); OLX was never live (pending a registered business account for real API credentials)
      - register.html — registration page: password strength meter, username availability check, Turnstile CAPTCHA widget
      - request-password-reset.html — "forgot password" email-request form
      - reset-password.html — password reset/set form (token from URL; `?mode=set` for first-time set)
      - setup-username.html — first-time username selection screen (post Google OAuth signup)
      - statistici.html — personal stats dashboard: achievements, visits, friends, favorites, owned consoles; level card has a "share my level" button, earned achievement cards have a share button
      - user-profile.html — public profile page for any username (server-routed SPA-style via `GET /user/:username`)
      - verify-success.html — email verification result page (forced English), handles resend
  - **js/**
    - **data/**
      - console-models.js — **MODELS moved to Postgres (2026-09-06, Phase 3 of the site-wide admin-editing task)** — this file no longer exports a static `MODELS` array; it now exports `loadModels()`/`invalidateModelsCache()` (fetches `GET /api/console-models` once, caches, same shape as `data-loader.js`'s `loadConsoles()`/`invalidateCache()` — bare `/api/...` path, no `API_BASE_URL` import, matching that sibling file's own convention) — see `backend/routes/console-models.js`. All 209 entries were migrated as-is (same order/wording, via a one-off script) into the new `console_models` table (`id`/`mfr`/`console`/`code` UNIQUE/`note`/`updated_by`/`updated_at`) so admins can add a new model and edit an existing one's fields directly from the site instead of editing this file and redeploying. `MOD_OPTIONS` stays a static export here, untouched. The research history below (194→200→209, PS2 gap-fill) describes data that's now IN THAT TABLE, not in this file — still accurate as the record of what was researched and why, just not the current storage location: (2026-09-06, two passes — PS2 model list turned out to have more gaps than the 2026-07-20 194-model audit caught: pass 1 added 6 missing Australia SKUs (`SCPH-39002`/`50002`/`70002`/`75002`/`77002`/`79002`, 194→200); pass 2, after Andrei spotted `SCPH-77003` missing, found ~25 more candidate PS2 codes via a deeper source (playstationgeneration.it's dedicated SCPH-3xxxx page + cross-referencing), of which 9 were confirmed via independent/primary sources and added (200→209): `SCPH-30005R`/`50005` (South Korea), `SCPH-30007R` (Taiwan), `SCPH-35002`/`35003` (Australia/UK, confirmed via playstationgeneration.it's dedicated 3xxxx-series page — the summary page's "SCPH-35005" and "SCPH-15000 TI" did NOT reappear there, treated as scraping artifacts, not added), `SCPH-39008` (CIS/Baltic — supports Cyrillic text but no selectable Russian UI, an official Sony PDF documents `SCPH-30005R`'s Korea launch specifically), `SCPH-70011` (NA/Canada, actually functionally distinct — built-in dial-up modem, confirmed via search), `SCPH-77003` (UK, matches the established 0/1/2/3/4 JP/NA/AU/UK/multi-region pattern for this generation), `SCPH-90010` (confirmed real, same Nov 2007 launch batch as the rest of `SCPH-9000x`, exact country NOT confirmed — noted honestly in its own entry rather than guessed). ~15 more candidates (`SCPH-35005`, `39003`/`39006`/`39007`, `50003`/`50006`/`50007`, `70003`/`70006`/`70007`, `75002A`/`75003`, `79001A`, `90003`/`90005`/`90007`–`90009`) remain UNRESOLVED — sources conflict or are silent on their exact region, deliberately not guessed; needs another research pass (maybe a way to fetch psdevwiki.com/consolemods.org, both 403 blocking automated fetches during this session). Also exports `MOD_OPTIONS` (since 2026-08-07): a static, per-`console`-value menu of pickable flash types + firmware version brackets for console-model.html's modding-guide selector — currently `'PS3'`/`'PS3 Slim'`/`'PS3 Super Slim'` — deliberately just a pick-list (no compatibility claim, no research citation baked in; firmware brackets are the ones the site owner specified directly) so the selector is visible before any write-up exists for most combinations. **`flashTypesForModel(consoleName, code)` (2026-09-08, new export)** — a PS3 board only ever has ONE flash chip, so the generic per-line `flashTypes` list (e.g. `'PS3'` = `['NAND','NOR']`) let a visitor pick a type a given specific board never had; this narrows it per real model code (PS3 Developer Wiki, Hardware flashing/SKU Models pages — psdevwiki.com blocks automated fetches, sourced via search instead): Fat `CECHA`/`B`/`C`/`E`/`G` → NAND, `CECHH`/`J`/`K`/`L`/`M`/`P`/`Q` → NOR; Super Slim `CECH-4000A`/`4001A` (the 12GB no-internal-HDD variants — their own `note` text already says "12GB flash", corroborating) → eMMC, everything else Super Slim → NOR; PS3 Slim is uniformly NOR already, no override needed. Falls back to the unnarrowed per-line list for any other console or an unrecognized PS3 code. `firmwareVersions` left as the generic per-line list — narrowing that too would need real per-firmware/flash-type compatibility research, not done here. Consumed by `console-model.js`'s `applyCombos(rows, consoleName, code)` (gained the `code` param the same day). PS3 `CECHJ`'s `note` field mentions its CFW guide — no new entry was added for CECH-J03 specifically, the existing `CECHJ` group entry is reused as-is. Includes real board-level revisions that hide behind what looks like a regional code (e.g. PS2 `SCPH-30000` vs `SCPH-30000R` — the R is a reversed switch-connector board revision, not a region marker) and non-retail repair/refurb boards a technician could still encounter (Xbox 360 `Opus`/`Elpis`, used only by Microsoft to rebuild RRoD units). Deliberately does *not* enumerate every pure regional suffix where the hardware is identical (e.g. PS5 `CFI-1015A` vs `CFI-1016A`, PS3's per-country 2-digit suffixes) — only generation-level codes are listed there, since region-only codes carry no disassembly/repair difference. **Not in .gitignore's `data/` rule by accident** — the root `.gitignore` has a bare `data/` entry that matches this whole directory at any depth; pre-existing tracked files here are unaffected (gitignore doesn't retroactively untrack), but any *new* file added under any `data/` folder anywhere in the repo needs `git add -f` or it silently won't show as untracked. Worth narrowing that gitignore rule (e.g. to a specific top-level path) if it causes another surprise.
      - consoles-data.js — auto-generated embedded English console dataset (`window.CONSOLES_DATA`), do not hand-edit
      - consoles-de.json / consoles-en.json / consoles-es.json / consoles-fr.json / consoles-it.json / consoles-ro.json — per-language console encyclopedia source data (`consoles-en.json` canonical), imported into DB by `backend/js/import-consoles.js`
      - data-loader.js — centralized console-data loader: fetches `/api/consoles?lang=`, falls back to English/embedded data, caches per language. `resolveImagePath(imagePath)` (2026-09-08 fix) now passes an absolute `http(s)://` URL through unchanged instead of prefixing it with a relative `../../`/`/` path — needed because the admin edit modal's image upload (`console-detail.js`'s `openConsoleEditor()`) writes a full R2 URL into `console.image`, not a relative bundled-asset path like the seed data always has; found while porting the console admin-edit feature to the Android app, not from a report (no console's image had actually been replaced through the form yet). `console-detail.js`'s JSON-LD `image:` field got the same absolute-URL guard (was unconditionally prefixing with `https://consolenotebook.com/`).
      - image-dimensions-data.js — auto-generated `window.IMAGE_DIMENSIONS_DATA` (width/height per console image, for layout-shift-free `<img>`)
      - image-dimensions.json — source JSON behind `image-dimensions-data.js`
      - location-data.js — `window.LOCATION_DATA`: country/city list used by location pickers (marketplace, profile). Each country's `name` field is the Romanian name and is kept for backward compat, but no longer read directly for display — `community.js`'s `countryLabel()` resolves the translated name via i18n (`country_<CODE>` keys, all 6 languages, added 2026-07-23) instead. City arrays hold each city's *native* spelling (used as the stored/filtered value, unchanged) — see city-name-overrides.js for display-only translations of the ones that have a real exonym.
      - city-name-overrides.js — `window.CITY_NAME_OVERRIDES` (added 2026-07-23): display-only translations for the ~35 cities in location-data.js that have a real, established name in another language (e.g. "München"→"Munich" in English, "Moskva"→"Moscova" in Romanian) — most cities don't and render as-is. Keyed by the exact native-spelling string from location-data.js; per-language sub-keys are optional (missing ones fall back to the native spelling). Resolved via `community.js`'s `cityLabel()`, never changes the underlying stored/filtered value. Loaded right after location-data.js on every page that includes it (community.html, profil.html, community-welcome-page.html, user-profile.html).
    - **fallback/**
      - comparatie.js — no-ES-module fallback for the comparison page (hamburger menu + comparison logic) for `file://` usage
      - console-detail.js — no-ES-module fallback that fetches console JSON and renders a console detail page dynamically
      - contact-form.js — no-ES-module fallback contact form handler with validation, Turnstile CAPTCHA widget, + Nodemailer-backed API call
      - quiz.js — no-ES-module fallback: auto-transforms static quiz HTML sections into interactive scored quizzes
      - search-profile.js — no-ES-module fallback: advanced search (consoles→people→marketplace→pages) + profile dropdown
    - **modules/**
      - achievement-socket.js — connects Socket.io client, joins user's room, shows toast on `achievement_unlocked` events
      - achievements.js — achievement/level display logic and toast notifications (definitions come from `gamification-data.js`)
      - animations.js — scroll-triggered fade-in animations via IntersectionObserver
      - auth.js — client-side session/API wrapper: login/register(terms/privacy/turnstileToken)/logout, JWT + localStorage, `getCurrentUser()`/`isLoggedIn()`
      - avatar-cropper.js — WhatsApp-style circular avatar crop modal; resolves a 512×512 JPEG Blob
      - contact-form.js — ES-module contact form validation + submit (module-enabled environments)
      - custom-player.js — custom YouTube IFrame API video player with play/pause/seek/volume controls
      - diacritics.js — normalizes common Romanian words typed/rendered without diacritics
      - gamification-data.js — frontend copy of XP/level/achievement constants (`window.GAMIFICATION_DATA`), must mirror `backend/utils/gamification.js`
      - home-timeline.js — animates the quick-start guide timeline fill bar on the home page, clickable steps
      - home.js — home page logic: user greeting, avatar URL normalization, achievements/auth wiring; also loads the Progress panel's "Top Contributors" leaderboard preview (`GET /api/leaderboard?limit=5`); the Quick Start Guide overlay doubles as a sequential onboarding tour (Next/Skip through incomplete tasks, auto-started once per browser via `localStorage['cn_onboarding_tour_dismissed']`); the favorited-listings preview widget shows a "Restored by CNote" badge for official listings; "My Listings" (Collection panel) always renders now instead of staying `hidden` when empty — shows a `.dash-empty-state` with `home_no_listings_text`/`home_no_listings_hint` when the user has no listings yet, built via the shared `myListingsEmptyStateHtml()` helper (also reused by the initial-render empty state, de-duplicating what used to be inline HTML in two places); `renderMyListingCards()` cards now have a `.hlc-btn--active` (🔒/🚫) button between edit/sold, toggling `active`/`inactive` via `PATCH /marketplace/listings/:id/status` (same generic endpoint the sold toggle already used — no backend change needed) and updating the shared INACTIVE/SOLD overlay badge (SOLD takes visual precedence over INACTIVE); fixed a latent `ReferenceError` in the delete-listing handler that referenced a since-removed `myListingsSection` variable — would have thrown the next time a user deleted their last listing — now uses `grid.innerHTML = myListingsEmptyStateHtml()` instead; fetches `GET /api/articles?limit=3` (added to the dashboard's `Promise.all`) and renders it into `#home-articles-preview` as `.article-card`s, or a `.dash-empty-state` if there are none yet; also fetches `GET /api/marketplace/listings?limit=4` (`trendingListingsRes`) and renders the new `#home-marketplace-preview` widget (see home.html's 2026-07-23 section-reorder entry above) the same way `renderMyListingCards()` builds a listing card, just read-only (no edit/sold/active buttons); removed two now-redundant "See all" links that used to be appended dynamically from JS (Friends' conditional `>8 friends` link in the Home-panel preview, Collection's conditional `>6 consoles` link) since both widgets' `<h2>` now carries a permanent one in the HTML — Achievements' own dynamic "X of Y earned" `.dash-see-all.btn--primary` link was deliberately left alone (richer than a generic link, so no static one was added to home.html for it). **(2026-08-21) "My Orders" panel** — fetches `GET /api/orders/mine` (added to the dashboard's `Promise.all` as `myOrdersRes`) and renders `#orders-grid`/`#orders-count`/`#orders-new-badge` via new nested functions `renderOrderCards(grid, orders)` (per-order `.order-card`: status badge, product+total, buyer name, copyable field rows for name/phone/delivery-method+address-or-easybox/notes, a "Copy all" button, and a ✅/↩️ ship-status toggle hitting `PATCH /api/orders/:id/status`) and `copyOrderField(text, btn)` (`navigator.clipboard.writeText`, briefly flips the button to ✓ — same simple try/catch-and-ignore failure mode as `share.js`'s `shareOrCopy()`, no execCommand fallback). Each card stashes its copyable values as a `data-copy-store` JSON blob (`.order-card__copy-values`) read by both the per-field and copy-all buttons — avoids re-deriving the delivery-method-dependent address/easybox string in two places. Added a local `timeAgo()` (duplicated from `community.js`'s, not shared — same as that file's own note about not having a shared module for this one function) for the card's relative date. `PANELS` array in the panel-hash-routing IIFE at the bottom of this file gained `'orders'`. **(2026-09-01)** the 3 listing-image fallbacks (My Listings, favorited, trending previews) now use `NO_IMAGE_PLACEHOLDER` from `utils/no-image-placeholder.js` instead of the never-committed `/assets/images/graphics/no-image-placeholder.jpg`.
      - i18n.js — i18n engine: `data-i18n` attribute translation, language persisted to localStorage; holds the `MESSAGES` object (en/ro/es/fr/it/de). (2026-08-21) +32 keys × 6 languages for the no-payment checkout feature (`sidebar_orders` + `order_*` — buy modal fields/summary/toasts, "My Orders" panel labels/status/copy buttons), inserted right after each language's `listing_generic_error` key. **(2026-09-01)** `community_chat_heading` retitled from "General chat"/"Chat general" to plain "Chat" (now labels the merged Chat+DM sidebar section, see `community.js`'s chat/DM merge note below); new `community_chat_unified` (💬 Chat, the merged sidebar item's own label) added; `community_messages_heading`/`community_messages_direct` removed (the "Messages" section they labeled no longer exists)
      - marketplace.js — shared marketplace helper functions (provider display name/icon, etc.)
      - model-directory.js — `initModelDirectory({gridId, searchId, tabsSelector, countId, emptyId, plateLabelKey, fromParam})` (now `async`, 2026-09-06): shared "Identify Your Model" hardware directory component (search/filter/tabs/grid of clickable `.model-plate` cards linking to `console-model.html?code=`) built from `await loadModels()` (`../data/console-models.js`, DB-backed now, was a synchronous static-array import before); used by both `pages/console-care.js` and `pages/console-modding.js` so the two guide pages don't duplicate this logic — both callers already fire-and-forget `initModelDirectory()` without awaiting it, so making it `async` needed no changes on their end. **Admin "Add model" (2026-09-06)**: `initAddModelButton()` inserts an admin-only trigger (`.model-directory-add-trigger`) right after the "N models" count line; `openAddModelForm()` opens a small modal (mfr/console/code/note) that `POST`s to `/api/console-models`, then `invalidateModelsCache()` + re-fetches + re-renders in place — no page reload.
      - navigation.js — smooth scrolling, active nav-link highlighting, mobile hamburger menu
      - notifications.js — notification bell dropdown: badge count, polling, mark-as-read
      - profile-dropdown.js — profile dropdown for logged-in/out states, language selector + theme switcher. **2026-07-25 — the "Default" (warm cinematic) theme was removed entirely, replaced with a "System" option**: `localStorage['cnote-theme']` now holds one of `'dark'`/`'light'`/`'system'` (no more `''`). `resolveTheme(stored)` turns the raw preference into a concrete `data-theme` value — `null` (never touched) → `'dark'`, `'system'` → whichever of dark/light matches `matchMedia('(prefers-color-scheme: light)')` at the moment, anything else passed through as-is; a `change` listener on that same media query keeps a live "System" choice in sync if the OS preference flips while the page is open. `_setTheme()` stores the RAW choice (so "System" stays correctly highlighted rather than freezing at whichever theme it last resolved to) but applies the RESOLVED value to `document.documentElement.dataset.theme`. The identical resolution logic is duplicated in the per-page inline theme-restore IIFE repeated in every HTML `<head>` (all 112 files, bulk-updated in one pass) and in profil.html's own Appearance-tab theme picker in `profile.js` — all three must stay in sync if this logic ever changes again. `frontend/css/base/variables.css`'s bare `:root` block (previously the reachable "warm cinematic" default) now just mirrors `[data-theme="dark"]`'s values as a safe no-JS/pre-hydration fallback, since a real page load always resolves to a concrete `dark`/`light` `data-theme` before first paint and the bare block is normally never visibly used.
      - progress.js — course lesson-completion tracking via localStorage, keyed per user
      - report-modal.js — DSA Article 16 content-report modal, `window.openReportModal(...)`, no framework
      - search.js — advanced global search: consoles → people → marketplace → forum threads → settings/pages, filter tabs, keyboard nav
      - sentry-init.js — initializes Sentry browser SDK with PII-scrubbing `beforeSend` hook
    - **pages/**
      - articles.js — articles.html controller: paginated list (`GET /api/articles`, "Load more"), admin-only "Write article" modal (title/excerpt/cover upload via `POST /api/uploads/presign` `kind: 'article'`/content HTML textarea/published checkbox) that `POST`s to `/api/articles` and redirects to the new article. Local `api()`/`showToast()` helpers mirror the pattern established in `console-model.js`/`community.js` rather than importing them (no shared module for either yet).
      - article.js — article.html controller: reads `?slug=` from the URL (same pattern as console-model.js), fetches `GET /api/articles/:slug`, renders cover/title/meta/`content_html` (into `.lsn-content`). Admin sees Edit (reuses the create modal's markup, prefilled, saves via `PUT /api/articles/:id`, redirects if the slug changed) and Delete (`utils/confirm-modal.js` → `DELETE /api/articles/:id` → back to articles.html) buttons.
      - card-enhancements.js — adds favorite-heart + community rating to console cards on the evolution/encyclopedia page
      - chat.js — community.html global chat: polling, message rendering, cooldown rate limit. **Bubble redesign (2026-09-02)**: `renderMessage()` now emits the same `.hub-dm-msg-row`/`.hub-dm-msg` bubble markup a DM message uses (`community.js`'s `renderMessageHtml()`) instead of its own `.chat-msg*` Discord-style rows — mine/theirs alignment via `msg.user.id === user.id`, a small `.hub-dm-msg__sender` name label above the first bubble of a run from someone else (general chat can have many senders, unlike a real 1:1 DM — mirrors the app's `showAuthor = newGroup && isGroup` in `ChatScreen.kt`), sticker attachments rendered via `.hub-dm-msg__sticker-img` if present. The character counter (`#chat-char-count`/`#char-current`) was dropped along with it — DM's own composer never had one either. `showEmptyState()` now just sets `#chat-loading`'s text to the `dm_first_message` key (element's class switched to `.hub-dm-empty` in the HTML) instead of building its own `.chat-empty-state` card. All the old `.chat-hdr*`/`.chat-msg*`/`.chat-box`/`.chat-input-pill*`/`.chat-empty-state*`/`.chat-typing*` CSS this replaced was removed from `community-hub.css` (confirmed unused elsewhere — the community-welcome-page's similarly-named `cl-chat-msg*` classes are a separate, unrelated namespace). **Mute block + fullscreen back button (2026-09-01)**: own `#chat-muted-notice` swapped in for `#chat-form` while muted (`refreshMuteStatus()`, 15s poll, same "cached session has no live muted_until" rationale as community.js's identical mechanism — separate copy since this is its own module scope). `#chat-back` (new, mobile-only) un-hides the site navbar/bottom-nav that `community.js`'s `showView('chat')` now hides — see `community-hub.css`'s `hub-mobile-fullscreen` notes. **(2026-09-01, chat/DM merge)** `#chat-back` now first tries `window.cnCommunityBackToChatList()` (exposed by `community.js`, returns to the merged chat list general chat is now reached from) and only falls back to the plain un-hide if that hook isn't present
      - community-welcome-page.js — logged-out community landing page interactivity (console category tiles etc.); `loadActiveCount()` drives both the hero pill (`#cl-stat-members`) and the "Community in numbers" → Active Now card (`#cl-stat-members-2`) from `GET /users/active-count` — the latter used to stay stuck at 0 since nothing wrote to it. `loadCategoryCounts()` sums per-category `GET /forum/:console/threads` calls into the "Forum Threads" stat (`#cl-stat-threads`) — its `consoles` list (2026-09-06 fix) must include `'general'` even though that category has no tile in the "Browse by platform" grid (`community-welcome-page.html` only has 5 `cl-cat-*` tiles: ps/xbox/nintendo/pc/other) — `community.js`'s own copy of this list (`validConsoles`, line ~3966) already included it, only this page's was missing it, so the stat stayed stuck at 0 even with real threads in the DB (all posted under `general`, e.g. the seed "Welcome" thread). **Live refresh (2026-09-06)**: `loadActiveCount()`/`loadCategoryCounts()`/`loadMarketplace()`/`updateOnlineCount()` (the "Community in numbers" stats + chat-header online count) previously only ran once on page load — now re-run every 30s via a `setInterval` (skipped while `document.hidden`, so a backgrounded tab on this public/unauthenticated page doesn't poll pointlessly) so the numbers actually move for a visitor lingering on the page instead of needing a reload.
      - community.js — Community hub controller: sidebar nav, forum, marketplace, repair wizard, DMs, Photos gallery; forum thread list cards and thread-detail/replies were redesigned Reddit-style (post-card look with a rounded vote pill `▲ count`, a 💬 reply-count pill, a ↗ Share pill reusing `shareOrCopy()`, and a views line using the `forum_threads.views` counter that already existed server-side) — the thread list (`loadThreads()`) renders `.hub-thread-card` (a `<div>`, not a `<button>`, since it now contains its own interactive pills; click-to-open is a delegated listener on the list that ignores clicks landing on `.hub-vote-pill`/`[data-share]`/`.report-trigger-btn`) with a truncated `body_snippet` preview; the thread detail (`openThread()`) builds a real nested reply tree via `buildReplyTree()` (groups the flat `replies` array by `reply_to_id`) and `renderReplyNode()` (recursive, wraps each reply + its children in `.hub-comment-thread` with increasing `margin-left`/`border-left` per depth, Reddit-style) — the old inline "Replying to X: snippet" quote text was removed since the visual nesting now conveys that; the reply form ("Join the conversation") moved from a sticky bottom bar to inline, directly under the original post and above the replies list; forum thread view supports quoting a specific reply ("Reply" button → `reply_to_id`) and thread-owner-only "mark as solution" (✓ Solved badge on the thread and the winning reply); listing detail shows seller reviews (average + list + leave-a-review star form, `loadSellerReviews()`) and a share button; Photos tab (`renderPhotos()`/`loadPhotos()`/`openPhotoUploadModal()`/`submitPhotoUpload()`) uploads via `POST /api/uploads/presign` (`kind: 'gallery'`) → direct PUT to object storage → `POST /api/community/photos`, reusing the same singleton `.hub-modal-overlay`/`.hub-upload-zone` dropzone component the "New listing" modal uses (not a bespoke modal — an earlier pass had accidentally duplicated `.hub-modal-overlay`/`.hub-modal`/`.hub-modal__close` under different styles later in `community-hub.css`, silently overriding the original component; fixed by reusing it instead); listing cards/detail show a "Restored by CNote" badge when `seller_is_official`; repair wizard's `SYMPTOMS_BY_CONSOLE` (must mirror `backend/routes/repair.js`'s `CONSOLE_SYMPTOMS`) includes a restoration option; the sidebar no longer has its own "Notifications" bell/dropdown (`initNotifications()` removed) — that duplicated the navbar's bell (`frontend/js/modules/notifications.js`), which already covers this page; listing detail's seller card is split into `.hub-detail-seller-info` (avatar+name) and `.hub-detail-seller-actions` (Contact/Share/Report) so it wraps instead of overflowing horizontally on mobile (was pushing the whole `#view-listing` into a horizontal-scroll state — every row appeared cut off on the left); DM view (`renderDM()`/`openConversation()`) toggles an explicit `#dm-layout.hub-dm-layout--thread-open` class instead of the old `:has(.hub-dm-thread:not(:empty))` CSS trick — the thread `<div>` always had a "Select a conversation" placeholder child so `:not(:empty)` was true from first render, permanently collapsing the conversation list to a 72px sliver on mobile; now mobile shows list OR thread full-screen with a `#dm-back-btn` (←) to return; `avatarHtml(name, url, size)` (used by forum thread/reply avatars, DM conversation avatars, and the listing-detail seller avatar) now gives every avatar-without-a-photo a deterministic per-username color via `avatarColor()`/`AVATAR_COLORS` — same hash+palette as `chat.js`'s `getUserColor()`, so a user's color is consistent between the community chat and every other avatar in the hub; `loadListings()`'s mobile card grid (`.hub-market-grid`, single-column "OLX-style" rows) had two compounding rendering bugs, both fixed in `community-hub.css`: (1) `.hub-market-grid` had no `grid-auto-rows` set, so once more than ~3 listings were on screen the implicit grid rows collapsed to fit the scroll container's visible height instead of sizing to content — every card got crushed shorter as more items loaded, with text bleeding into the row above (fixed with `grid-auto-rows: min-content` + `min-height: 0`); (2) `.hub-listing-info__title`/`__desc` use `display:-webkit-box` + `-webkit-line-clamp` for 2-line truncation while being flex children of `.hub-listing-info { display:flex; flex-direction:column }` — without `flex-shrink: 0` that combination renders with zero height in Chromium (title/description invisible even though the DOM/text is there), fixed by adding `flex-shrink: 0` to `.hub-listing-info__title`/`__price`/`__desc`; `openAddListingModal()`'s image upload no longer builds base64 data URLs to embed in the listing JSON — `resizeImage()` now resolves to a resized JPEG Blob, uploaded via `uploadListingImage()` (presign `kind: 'listing'` → direct PUT) which returns the storage key sent as `images` instead; `openNewThreadModal()` gained an optional single-photo upload (same presign-then-PUT pattern, `kind: 'forum'`) whose returned key is sent as `image_key` — shown as `.hub-thread-card__image` in both the thread list card and the thread detail's original-post block, via the `image_url` the backend resolves from that key; `uploadListingImage()` (listing images, `kind: 'listing'`) and the forum thread image upload in `openNewThreadModal()` (`kind: 'forum'`) are now wrapped in try/catch (return `null`/skip on failure instead of throwing) — an unguarded direct-PUT-to-object-storage call in either of these (e.g. CORS not yet configured on the bucket) was silently breaking the entire enclosing async submit handler via an unhandled rejection, leaving the Publish/submit button stuck disabled with no visible error; the submit handlers themselves also wrap the final `POST` call in its own try/catch with proper button re-enable + error toast on any failure — this is now the required pattern for any new presign-then-PUT flow in this file; listing detail's action row gained a `#listing-active-btn` (own, non-sold listings only) toggling `active`/`inactive` via `PATCH /marketplace/listings/:id/status` (mirrors the sold toggle, same generic endpoint, no backend change); DM view (`openConversation()`) gained a "+" attach button (`#dm-attach-btn`/`#dm-image-input`) for image attachments and a 🎤 mic button (`#dm-mic-btn`) for voice messages, matching the Android app's chat — images upload via presign `kind: 'image'` and voice recordings (captured via `MediaRecorder`, uploaded as a Blob on `stop`) via presign `kind: 'voice'` (both resolve to the `chat/images/`/`chat/voice/` key prefixes `routes/dm.js` validates against — NOT the newer `listing`/`forum` kinds), sent through `POST /dm/send` with `attachment_key`/`attachment_type`/`attachment_size`/`attachment_duration_ms`; a pending image shows as a removable chip in `#dm-pending` above the input before sending; message rendering shows `<img class="hub-dm-msg__image">`/`<audio class="hub-dm-msg__audio" controls>` per `m.attachment.type`; every upload/network step follows the same try/catch pattern as the listing-image fix above; the "New listing"/"Edit listing" modals (`openAddListingModal()`/`openEditListingFromDetail()`) previously had zero i18n coverage — every label/placeholder/option/button was hardcoded English (plus a couple of stray hardcoded Romanian strings like the title placeholder and the console-select default option, which is what caused the reported "half Romanian, half English" mixed-language bug) — fixed by wiring every string through `t('listing_*')` (new keys, all 6 languages), matching the inline-`I18nModule.t()`-in-template-literal pattern the Photos upload modal already used; `CONDITIONS`/`CATEGORIES` (module-level constants used by these modals AND by the condition/category badges on listing cards) now hold i18n key names instead of raw English strings, resolved via `t()` at render time; Marketplace view header (`renderMarketplace()`) no longer has a "💬 Messages" button (redundant with the sidebar's own Messages section) — the sort `<select>` moved up into its place next to "+ New listing", and the topbar below now only has the search input + Filters button; default view/route is now `marketplace` (was `chat`) — see `S.view` initial state and `handleHashNavigation()`'s fallback `navigate('marketplace', null, '')`; `handleHashNavigation()` also recognizes a dedicated `#marketplace/new` deep link (used by home.html's "Add listing" button) that navigates to Marketplace and immediately calls `openAddListingModal()`; marketplace country/city `<select>`s (filter drawer, New/Edit listing modals) and the read-only city line on listing cards/detail were showing `LOCATION_DATA`'s hardcoded Romanian country names and untranslated native city spellings regardless of site language — fixed 2026-07-23 via two new helpers, `countryLabel(country)` (→ `I18nModule.t('country_' + country.code)`, see location-data.js's entry above) and `cityLabel(city)` (→ `window.CITY_NAME_OVERRIDES[city]?.[lang]`, see city-name-overrides.js) — both fall back to the raw untranslated value, and neither changes the actual `<option value="...">`/stored `country`/`location` fields, only the displayed label. **2026-07-25 — DM view rebuilt for parity with the Android app's chat UI** (iMessage-style bubbles/reply/edit/delete/reactions/forward/stickers/full-screen zoom, "adapt the whole chat interface to the site" request): `renderMessageHtml(m, myId, partnerName, msgsById, prevMsg, nextMsg)` is now the single render path for every message (replaces the old inline `.map()` template in `openConversation()` plus the separate `appendSentMessage()`/mediaRecorder-stop/form-submit ad hoc HTML strings) — builds the reply quote (via `previewText()`, image/sticker thumbnails instead of text, same fallback idea as `ChatViewModel.kt`'s `previewText()`), attachment (image/custom waveform voice player/bare sticker), edited label, and reaction chips, with `stacked`/`tail` modifier classes for iMessage-style bubble grouping; `appendMessageToThread()`/`updateMessageInPlace()` do incremental DOM updates (append one message, or re-render one row after a reaction/edit) instead of re-fetching the whole thread. New standalone functions: `renderVoicePlayerHtml()`/`toggleVoicePlayback()`/`seekVoice()` (custom waveform player, one `Audio` shared/playing at a time via module-level `activeVoiceAudio`/`activeVoiceEl`, deterministic pseudo-amplitude bars seeded by message id — same idea as `VoiceMessageBubble` in `ChatScreen.kt`, not real decoded amplitude); `openMessageContextMenu()`/`openConversationContextMenu()` (native `contextmenu` right-click → floating quick-reactions row + action list, mirrors `MessageActionsMenu`/`ConversationActionsOverlay` in `ChatScreen.kt` — message actions are Reply/Forward/Copy/Edit(mine)/Delete(mine), conversation actions are local-only Pin/Mute/Delete-chat, see below); `openForwardPicker()` (modal listing conversations, reuses `.hub-modal-overlay`/`.hub-modal` chrome, sends via `POST /dm/send` reusing the original `attachment.key` — no re-upload, same tradeoff as `forwardMessage()` in `ChatViewModel.kt`, DMs only); `openImageViewer()` (full-screen `position:fixed` overlay — deliberately NOT a dialog/modal "window", since that's exactly the class of bug that cost six rounds of debugging on the Android side; wheel/pinch zoom via CSS transform, drag-to-pan when zoomed, swipe-down-to-dismiss when not, top bar back/name/forward, bottom reply-bar that sends straight into the open conversation). Local-only conversation state (pin/mute/hide — no backend concept on Android either, see `pinnedIds`/`mutedIds`/`hiddenIds` in `ChatViewModel.kt`) is persisted via `localStorage`, namespaced per user id (`dmLocalKey()`/`loadDmSet()`/`saveDmSet()`/`loadDmHidden()`/`saveDmHidden()`) — "delete chat" hides the conversation from the list until a message newer than the delete-time arrives, exactly like the app. Also fixed a drive-by bug found while rewriting `loadConversations()`: the unread badge checked `c.unread_count` but the backend field is actually named `unread` — the badge had never once rendered. New sticker send flow: 🎨 button opens `#dm-sticker-panel` (fetches `GET /api/stickers`, click sends `attachment_type: 'sticker'` reusing the library's existing storage key, no upload). All new user-facing strings added as `dm_*` i18n keys (all 6 languages) in the same pass, including converting several previously-hardcoded-English strings in this view (`dm_login_required`, `dm_loading`, `dm_no_conversations`, `dm_select_conversation`, `dm_write_placeholder`, etc.) and fixing a stray hardcoded-Romanian fallback name (`'Utilizator'` → `t('dm_unknown_user')`). Verified end-to-end via a real logged-in Playwright browser session against production data (send/reply/edit/delete/react/forward/sticker-panel/context-menus/full-screen viewer all confirmed working) — this surfaced and fixed two real bugs: (1) `reply_to` silently never matched (see `routes/dm.js`'s entry above — BIGINT-as-string from `pg`); (2) `positionCtxMenu()` could push the context menu off the bottom of the viewport when right-clicking a message near the bottom of the thread — fixed to flip and open upward from the click point when there isn't room below (plus a `max-height`/`overflow-y:auto` safety net in `.hub-ctx-menu`, community-hub.css). **2026-07-25, second pass — 3 more bugs reported after real usage, all fixed**: (1) the compose form's image-upload try/catch used to swallow a failed presign/PUT silently and fall through to sending the typed text as a *plain* message with no attachment and no error — looked exactly like "attaching a photo does nothing"; fixed to abort the send entirely on upload failure, restore the pending-image chip + typed text + reply state untouched for retry, and show a `dm_image_upload_failed` toast (new i18n key, 6 languages) — verified locally by attaching a file with R2 intentionally unconfigured (guaranteed real upload failure) and confirming nothing was sent; (2) DM has no realtime transport (see Socket.io note above — achievement/notification-only), so a partner's new messages/edits/reactions never appeared without a manual reload; added `syncDmMessages()` + a 4s `setInterval` (`DM_POLL_INTERVAL_MS`) while a conversation is open — mirrors `chat.js`'s own polling pattern rather than inventing a new one, diffs against `S.dmMessages` (appends new messages, in-place-updates edited/reacted ones via `updateMessageInPlace()`, drops ones deleted server-side), stopped via `stopDmPolling()` on back-button/switching conversations/navigating to any other sidebar view (hooked into `showView()`); verified by sending a message through the raw API (simulating another device) and confirming it appeared without a page reload; (3) "edge-to-edge fade" was asked for the DM thread's own header/compose-form bars too, not just the full-screen image viewer — `.hub-dm-thread__header` is now `position: absolute` (floats over the scrollable message list with a dark-to-transparent gradient + `backdrop-filter: blur()`, `pointer-events: none` except its own children so clicks/scroll still reach messages underneath — same idea as `PageScaffold.kt`'s header treatment on the Android side) and `.hub-dm-form` got the equivalent bottom-up fade instead of a flat bordered bar; `.hub-dm-messages` gained top padding to clear the header's visible button row. Had to fix this in THREE places in `community-hub.css` (base rule + two separate, overlapping `@media (max-width: 768px)` blocks that already existed for mobile — the second, later one was winning the cascade and had hardcoded solid backgrounds, which is what the reported screenshot was actually showing). **2026-07-25, third pass — corrected per follow-up feedback**: (1) the blur-based fade above was replaced with the *exact* `.navbar`/`.navbar::after` pattern (see `frontend/css/layout/navbar.css`) — the header/form themselves are `background: transparent` with no `backdrop-filter` at all, and a separate `::after` pseudo-element (extending 30px beyond the bar, `z-index: -1`) carries the `linear-gradient(..., rgba(var(--dark-rgb), ...), transparent)` fade — crisp, no blur, reusing the theme's own `--dark-rgb` variable instead of hardcoded black; (2) `#dm-compose-banner`/`#dm-sticker-panel`/`#dm-pending` were staying visible as empty bars even while their `hidden` attribute was set, because `.hub-dm-reply-banner`/`.hub-dm-sticker-panel`/`.hub-dm-pending` each set `display` unconditionally — an author-stylesheet rule at equal specificity always beats the browser's own `[hidden] { display: none }` UA rule. Fixed by scoping all three to `:not([hidden])`. **2026-07-25, fourth pass — two more real bugs from continued usage**: (1) the compose form's fade tail (`.hub-dm-form::after`, `z-index: -1`) was getting visually cut off by the last message bubble instead of showing through like the header's — root cause: `.hub-dm-form` had `position: relative` but no explicit `z-index` of its own, so it never established a real stacking context, letting the `::after`'s `z-index: -1` escape upward and get evaluated against `.hub-dm-messages`' bubbles (`z-index: auto`) instead of staying scoped to just the form — fixed by adding `z-index: 2` to `.hub-dm-form` itself (mirrors the header, which already had `z-index: 3` and never had this problem); (2) sending a reply from inside the full-screen image viewer's bottom bar sent a plain message with no `reply_to` at all, so it never actually quoted the photo being viewed — `openImageViewer()`'s `#viewer-reply-send` handler now includes `reply_to: m.id`. Also redesigned the whole compose bar to match `Composer()` in `ChatScreen.kt` exactly, since it was still the site's own ad-hoc layout (separate square-ish icon buttons + a bordered rectangular input + a text "Send" button): round 44px `+`/🎨 buttons (`.hub-dm-form__round-btn`, renamed from `.hub-dm-form__icon-btn`) sit next to one pill-shaped field (`.hub-dm-form__field`) that contains the *actual* input plus a single merged mic/send button (`.hub-dm-form__mic-send-btn`) living inside the pill — the icon swaps from 🎤 to a round accent-colored ➤ the moment there's text or a pending image (`updateComposerButton()`, plus `syncComposerSendIcon()` for the module-level `startEdit()`/cancel-compose paths that can't reach that closure), matching the app's `sendEnabled` swap precisely; clicking it either submits the form or toggles voice recording depending on which state it's in. **2026-07-25, fifth pass — fade still clipping content, plus icon/design parity request**: the topmost message (visible at rest when scrolled to the start of a conversation) sat under the tail end of the header's `::after` gradient, which wasn't fully transparent yet at that point — `padding-top: 60px` on `.hub-dm-messages` only cleared the header's own ~58px box, not the extra 30px the fade tail extends past it, so the first message (most visible on a sticker, which has no opaque bubble background to begin with) looked visually darkened/cut at its top edge; same issue existed symmetrically at the bottom against the form's fade. Fixed by bumping `.hub-dm-messages` padding to clear the FULL fade zone (header/form box + 30px tail) in all three places (base rule + both mobile media queries). Also aligned icons/colors with `ChatScreen.kt`'s `Composer()` exactly: the sticker button was 🎨 (palette) but the app uses `Icons.Default.EmojiEmotions` (a face) — changed to 😊; the send icon was ➤ (right arrow) but the app uses `Icons.Default.ArrowUpward` — changed to ↑; `.hub-dm-form__round-btn` (the +/sticker buttons) is now persistently accent-tinted (`color: var(--accent-color)`) instead of only on hover, matching the app's icons which are always `AccentGold`-tinted, not conditionally so (note: this only visibly affects the "+" glyph, since color-emoji glyphs like 😊/🎤 ignore the CSS `color` property — an inherent limit of using emoji instead of a real icon set). **2026-07-25, sixth pass — the compose bar was still visually a hard-flow bar, not a real overlay**: user feedback was explicit — "content passing underneath it should be visible, not cut." The header had already been made a floating `position: absolute` overlay (messages scroll fully behind it, fade showing through), but the compose form was left in normal document flow with just a transparent background — messages never actually scrolled underneath it, they simply stopped above it, so there was nothing for the fade to show "through". Restructured: `#dm-compose-banner`/`#dm-sticker-panel`/`#dm-pending`/`#dm-form` are now wrapped in a single `.hub-dm-compose-dock` (`position: absolute; bottom: 0; z-index: 3`, same treatment as the header) — the `::after` fade tail moved from `.hub-dm-form` to this wrapper so it's anchored to whatever is currently the dock's topmost visible edge (the reply banner or sticker panel when open, the form otherwise) rather than always sitting above just the form row. `.hub-dm-messages` now occupies the thread's full height and gets bottom padding sized to clear the dock's resting height + fade tail, so the last message genuinely scrolls out of view behind the translucent dock instead of stopping at a hard line above it. **2026-07-25, seventh pass — fade removed from the compose dock entirely**: per direct feedback ("looks ugly, better without") — `.hub-dm-compose-dock::after` deleted outright (the header keeps its own fade tail, unaffected); `.hub-dm-messages`' bottom padding reduced back down to just clear the dock's resting height (no more buffer for a fade tail that no longer exists) in all three places it's set. **2026-07-25, eighth pass — mobile fullscreen for detail views**: on mobile, an open DM conversation, a forum thread detail, and a marketplace listing detail already have their own in-page back control, so the site's fixed `.navbar` and the floating `#mobile-bottom-nav` pill are now hidden while any of the three is open — new `setMobileFullscreen(active)` toggles the *same* `navbar--hidden`/`mbn--hidden` classes `navigation.js`'s scroll-based auto-hide already uses (so it composes with that behavior instead of fighting it) plus a `body.hub-mobile-fullscreen` class that reclaims `.community-page--hub`'s reserved top padding on mobile. Called from `showView()` itself for forum/listing (`id === 'thread' || id === 'listing'` — this naturally also turns it back off when navigating to any other view) and explicitly from DM's `openConversation()`/back-button/delete-chat-while-open paths, since DM's thread-open state isn't a distinct `S.view`. Verified live at a 390×844 viewport: navbar/bottom-nav hidden while any of the three detail views is open, restored correctly after using each view's own back control. **2026-07-25, ninth pass — two real bugs from real-device testing**: (1) `setMobileFullscreen()` toggled `navbar--hidden`/`mbn--hidden` unconditionally — those classes have no media-query guard of their own (only `navigation.js`'s own scroll-based auto-hide checks viewport width before adding them), so opening a DM/thread/listing detail on a *desktop*-width window also hid the navbar there; fixed by checking `matchMedia('(max-width: 768px)')` inside `setMobileFullscreen()` itself before ever applying the hidden classes. (2) Even with the navbar/bottom-nav actually hidden, the detail view still looked like a rounded card floating inside the screen rather than genuinely fullscreen — `.hub-content`'s own `padding: 12px 14px 12px 12px` plus `.hub-view`'s `border-radius: 16px` (both sensible for the desktop "card" look, inset within the sidebar layout) were still in effect; `body.hub-mobile-fullscreen` now zeroes both on mobile. Verified live: desktop navbar confirmed to stay untouched while a DM thread is open, and `#view-dm`/listing-detail now measure the full viewport with zero inset/radius on mobile. **2026-07-25, tenth pass — one more source of the same bottom gap, found via direct DOM measurement rather than guessing again**: a completely separate, differently-breakpointed rule (`@media (max-width: 680px) { .community-page { padding-bottom: 90px; } }`, near the toast-notification styles far below the rest of this mobile work) was still reserving 90px at the bottom — `.community-page--hub` carries the plain `.community-page` class too (`class="community-page community-page--hub"` in `community.html`), so this rule applied on top of everything already fixed above. `body.hub-mobile-fullscreen .community-page { padding-bottom: 0 }` added inside that same `680px` block. Verified via `getComputedStyle` + `getBoundingClientRect` on every ancestor from `.community-page--hub` down to `.hub-dm-form` — all now measure/end at the exact viewport edge (844px), not 754px. **2026-07-25, eleventh pass — horizontal scrollbar + bubble text wrapping oddly**: two separate bugs, one real and one that turned into a genuinely stubborn CSS quirk. (1) `.hub-dm-reply-quote__text` had a fixed `max-width: 200px` that didn't shrink for narrow phones — on a narrow "mine" bubble (right-aligned, itself capped at 75% of viewport) the quote text could poke ~11px past both the bubble AND the screen edge, which (combined with `overflow-y: auto` implicitly making `overflow-x` compute to `auto` too per spec) surfaced as a real horizontal scrollbar on `.hub-dm-messages`; fixed to `max-width: 100%` (relative to its actual flex container) plus an explicit `overflow-x: hidden` safety net on `.hub-dm-messages` itself. (2) Tried adding `overflow-wrap` (both `anywhere` and `break-word`, on `.hub-dm-msg` directly and on a dedicated `.hub-dm-msg__text` child) to stop a pathologically long unbroken string from doing the same thing — every variant instead made Chromium size the bubble's flex-item auto-width far narrower than short *ordinary* text needed, so even "mgrorg" (no overflow risk at all) wrapped onto 2 lines despite plenty of room within the 75% cap; verified via direct `getComputedStyle`/`getBoundingClientRect` inspection that the bubble was measuring ~64px regardless of which element got the property. Not worth fighting further — reverted `overflow-wrap` entirely; a genuinely pathological unbroken string is instead just visually clipped by `.hub-dm-messages`' `overflow-x: hidden` (no scrollbar, no bubble distortion) rather than wrapped, which is an acceptable tradeoff since real messages always contain spaces. **2026-07-25, twelfth pass — bubble still rendered smaller than its own text after user's own follow-up commits (`38b74ac8`/`29e88f49`, made directly by the user, not this assistant — left untouched)**: `.hub-dm-msg` relied entirely on flex cross-axis shrink-to-fit sizing (a bare `width: auto`, sized only via `.hub-dm-msg-row`'s `align-items: flex-end/flex-start`) with no explicit width of its own, which left it vulnerable to being stretched wide by any ancestor `align-items` default (`.hub-dm-messages` itself never sets `align-items`, so its default is `stretch`); made it explicit and un-overridable instead: `.hub-dm-msg { width: fit-content; max-width: 100%; }`, so the bubble can never render wider than its own content regardless of what an ancestor's alignment computes to. Also added the same navbar-style fade the DM thread header already has (`.hub-dm-thread__header`/`::after` pattern) to `.hub-view-header` — but scoped ONLY to `body.hub-mobile-fullscreen #view-thread`/`#view-listing` (forum thread detail / marketplace listing detail, the two other views `setMobileFullscreen()` already treats as fullscreen), not the ~10 other views sharing the same `.hub-view-header` class (repair wizard, admin, etc.) which keep the plain solid card-header look; `.hub-thread-detail`/`.hub-detail-scroll` gained matching `padding-top: 84px` in that same scope since the header is now a floating overlay instead of a normal-flow bar. Not yet re-verified live against a real browser (no headless browser available in this environment this pass — Windows-side `node.exe` dev server is unreachable from WSL via `localhost`/127.0.0.1 despite `netstat` showing it LISTENING on `0.0.0.0:3000`, only reachable via the WSL default-gateway IP; confirmed via direct `diff` against `https://consolenotebook.com/css/pages/community-hub.css` that production *is* running this exact file, i.e. Railway auto-deploys from `main` and the deployment-topology concern from earlier passes is resolved) — ask for a fresh screenshot after this deploys. **2026-07-25, thirteenth pass — real-phone-only bug, not reproducible in devtools responsive mode**: user reported the marketplace-listing/forum-thread "fullscreen" fix only worked in desktop devtools emulation — on an actual phone the whole page scrolled and the navbar/bottom-nav reappeared at both top and bottom. Root cause found by installing a real local Playwright+Chromium test rig (see below) and reading `reset.css`/`navigation.js` directly rather than guessing: `reset.css`'s global `body { min-height: 100vh }` fights `.community-page--hub { height: 100dvh }` (added in the eighth pass) — on a real phone `100vh` includes the space behind the browser's collapsible address bar while `100dvh` doesn't, so `body` ends up a few dozen px taller than the actually-visible viewport whenever the address bar is showing (devtools' responsive mode doesn't simulate this at all, which is why it looked fine there). That stray sliver made the *whole page* scrollable, which fed `navigation.js`'s scroll-driven `setupAutoHideNavbar()` — its `onScroll` handler had no awareness of `body.hub-mobile-fullscreen` at all, so any scroll (even a few px of iOS rubber-banding) could call `showAll()` and re-show both the navbar and bottom-nav regardless of what `setMobileFullscreen()` had explicitly set. Fixed both: `body.community-navbar-page { min-height: 100dvh }` added alongside the existing `@media (max-width: 768px)` hub rules (community-hub.css), and `onScroll()` now returns early (same as the existing `menu-open` guard) whenever `document.body.classList.contains('hub-mobile-fullscreen')` (navigation.js). **New capability established this pass — local Playwright testing now actually works**: installed `playwright` (`npm install --no-save playwright` — Windows npm, not WSL's) + `npx playwright install chromium`, driven via Windows `node.exe` directly (`"/mnt/c/Program Files/nodejs/node.exe" script.js`, NOT WSL's `node`, which doesn't exist here) — this reaches the locally-running `npm start` server over `127.0.0.1:3000` directly (unlike `curl` from WSL, which can't reach it at all; see [[local_dev_server_testing]] gotcha #4, now superseded — Playwright's OWN Chromium process is Windows-side too, so plain `127.0.0.1` just works, no gateway-IP workaround needed). Two more gotchas found and worked around: (a) the existing `x-forwarded-proto: https` header spoof (to dodge server.js's forced-HTTPS redirect, see gotcha #6) must NOT be applied via `page.setExtraHTTPHeaders()` (applies to every request including cross-origin CDN calls) — it breaks the CORS preflight for Google Fonts/DOMPurify and silently kills navbar/footer injection; instead use `context.route('**/*', ...)` and only inject the header when the request's `hostname === '127.0.0.1'`, letting cross-origin requests through unmodified; (b) `/community.html` (and presumably other authed pages) renders essentially nothing (`.hub-content` never appears) for a logged-out session — real test credentials are required to drive this page meaningfully, not just anonymous `page.goto()`. This pass proceeded without login (user declined to share credentials for this round — "skip login, just reason from code") once the actual root cause had already been found by direct code reading, but the working Playwright setup is now available for a future pass that does have a login. **2026-07-25, fourteenth pass — the thirteenth pass's fix wasn't enough on a real Samsung phone**: user reported that scrolling far enough for the *browser's own* chrome (Samsung Internet/Chrome's collapsible address bar / gesture-nav bar) to hide still brought the site's navbar back on all 3 fullscreen views. Root cause: `.community-page--hub`'s mobile rule used `height: 100dvh` on a normal-flow flex child of `body` — `dvh` is recalculated on viewport resize, but there's a window where `body`'s own rendered height (`min-height` from reset.css, itself patched in the thirteenth pass) and this child's freshly-recalculated `100dvh` can still momentarily disagree during the actual resize *transition* triggered by the browser chrome hiding/showing mid-scroll (not just the earlier static vh-vs-dvh gap) — enough to make the page scroll by a few px and re-trigger `navigation.js`'s scroll-driven auto-hide despite the `hub-mobile-fullscreen` guard added in the thirteenth pass. Fixed at the root instead of patching the symptom again: `.community-page--hub` is now `position: fixed; inset: 0` on mobile (was `height: 100dvh` on a `position: relative`, in-flow box) — a fixed, inset-0 box always exactly matches whatever the visual viewport currently is, with no recalculation lag, and removes the element from `body`'s flow entirely so `body`'s height/scrollability can no longer be affected by it at all, regardless of what the browser's chrome is doing. `body.community-navbar-page { min-height: 100dvh }` (thirteenth pass) kept as a belt-and-suspenders backstop, no longer load-bearing on its own. Not verifiable in Playwright/devtools either way — headless Chromium has no real collapsible browser chrome to hide, so this class of bug can only be confirmed on an actual phone; reasoned through from first principles (CSS positioning/containing-block semantics) rather than observed. **2026-07-25, fifteenth pass — scoping correction**: the fourteenth pass's `position: fixed` change was applied to the base (unconditional) mobile `.community-page--hub` rule, so it affected every community view on mobile, not just the 3 fullscreen ones — user caught this ("only the 3 pages, not all of community?"). Moved it: base rule reverted to plain `height: 100dvh` (list views — chat/forum list/marketplace list/DM list/photos/repair — unaffected, unchanged from before the fourteenth pass), and `position: fixed; inset: 0; height: auto` (the `height: auto` is required here to cancel the base rule's `height: 100dvh`, which would otherwise over-constrain a fixed box with both top/bottom pinned to 0) now lives inside the existing `body.hub-mobile-fullscreen .community-page--hub` rule instead, matching `setMobileFullscreen()`'s own scope (DM thread / forum thread / listing detail only) exactly. **2026-08-21 — Buy button + no-payment checkout modal**: listing detail's action row gained a `#listing-buy-btn` (🛒, shown when logged in, not own listing, not sold) opening `openBuyListingModal(l)` — a new `.hub-modal-overlay` (same chrome as `openEditListingFromDetail()`) collecting Sameday shipping data: recipient name/phone, a delivery-method toggle (`.order-method-btn`, Address vs Easybox), conditional fields (județ/localitate/adresă for Address — `ROMANIAN_COUNTIES`, a plain static 42-entry list since Sameday is domestic-only, not the multi-country `LOCATION_DATA`; a free-text locker name for Easybox, since there's no live Sameday locker-lookup API), optional notes, and a live `.order-summary` (product price + flat shipping `ORDER_SHIPPING_PRICE` — 25 RON address / 20 RON easybox — + total) that recalculates when the method toggle changes. Submits `POST /api/orders`; on success closes the modal, toasts `order_success_toast`, and reloads the listing detail (now shown sold). See `routes/orders.js` for the backend and `home.js`'s new "My Orders" panel for where the seller processes it. **Mute now also blocks "New thread" (2026-09-01)**: `openNewThreadModal()` checks the new module-level `mutedHoursRemaining()` (own `muted_until`, refreshed every 15s by `startMuteStatusPolling()` since the cached session object never carries a live value) and shows a toast instead of opening the modal when muted — mirrors `forum.js`'s existing server-side 403. The DM composer (`openConversation()`'s `#dm-form`) is swapped for a `#dm-muted-notice` the same way via `applyDmMuteUI()`, called after every conversation render — covers every way a DM gets opened (there's no single "New chat" button on the site the way the app has one; whichever page starts a conversation, it always funnels through this same function). **Chat now fullscreens on mobile too (2026-09-01, Andrei: "cand intru pe chat, nu e fullscreen, imi apare navbaru")**: `showView()`'s `setMobileFullscreen()` call gained `id === 'chat'` — previously only thread/listing/DM did; Chat has no "parent" view to return to (it's a direct bottom-nav destination), so a new `#chat-back` button (mobile-only, `.chat-hdr__back` CSS) just un-hides the navbar/bottom-nav in place instead of navigating anywhere — wired in `chat.js` directly (inline, not imported from community.js — separate script/module scope). **Report-button fixes (2026-09-01)**: three `report-trigger-btn`s (thread-list card, thread-detail card, forum reply) rendered only a bare "⚑" glyph — the real label (`report_btn_trigger`, already `'⚑ Report'`) was passed as a `title` tooltip only, never the visible text, and the button had no pill/background styling either (small gray text among proper `.hub-action-pill`-styled Vote/Share pills) — easy to miss entirely, which is what read as "there's no report button". Now render the i18n string directly as the button's text and carry `.hub-action-pill` (thread cards) / `.hub-comment__action` (replies) so they look like actual buttons. The listing-detail report button had the opposite bug — a literal `'⚑ '` prefixed onto `report_btn_trigger_listing` (already `'⚑ Report listing'`) doubled the flag — fixed, and given `.hub-btn.hub-btn--secondary` to match the Contact/Share buttons beside it. The DM hover report button (bare "⚑", `report_btn_trigger_dm_title` with no flag in it) was left as-is — that one's a deliberate compact hover-corner icon, not a bug. **Chat/DM merge (2026-09-01)**: general community chat is no longer reached via its own sidebar/bottom-nav entry — `loadConversations()` now prepends a synthetic pinned "General" row (`data-id="general"`, `.hub-dm-conv--general`) to the real DM conversation list, with a one-line preview fetched via new `fetchGeneralPreview()` (`GET /chat/messages?limit=1`, best-effort, never throws); clicking it calls `navigate('chat')` (full swap to the existing `view-chat`/`chat.js` panel, contextmenu/pin/mute are skipped for it since it isn't a real conversation row). `window.cnCommunityBackToChatList` is exposed (`showView('dm'); renderDM(); loadConversations();`) so `chat.js`'s separate-module-scope `#chat-back` button can return to the merged list instead of just un-hiding the nav in place. Mirrors the Android app's `ChatViewModel.kt`, which folds general chat into the same conversation list as a pinned `GENERAL_ID` row. Listing-image fallback (marketplace grid + similar-listings) now uses `NO_IMAGE_PLACEHOLDER` (`utils/no-image-placeholder.js`) instead of the never-committed `/assets/images/graphics/no-image-placeholder.jpg`. **(2026-09-02) `#view-chat` removed** — general chat no longer full-page-swaps away from the list (Andrei: it rendered oddly under the navbar, and on desktop opened full-page instead of beside the list). `#chat-thread` (chat.js's markup) is now a permanent static sibling of `#dm-list`/`#dm-thread` inside `#view-dm`'s `.hub-dm-layout` (community.html) — only one of `#dm-thread`/`#chat-thread` is ever visible, toggled via the `hidden` attribute by new `openGeneralChat()`/`closeGeneralChat()` (mirroring `openConversation()`'s own thread-open/back-button logic exactly, including `stopDmPolling()`/`stopActiveVoice()` and `setMobileFullscreen()`). New `S.generalChatOpen` flag persists which pane was open across a `loadConversations()` refresh, same as `S.dmPartner` already did for DM. `renderDM()` no longer replaces `#view-dm`'s entire `innerHTML` each visit (that would have destroyed `#chat-thread`'s DOM, permanently orphaning chat.js's element references bound at module load) — it only resets `#dm-list`/`#dm-thread`'s own inner content now. `window.cnCommunityBackToChatList` (chat.js's `#chat-back` hook) now calls `closeGeneralChat()` directly instead of a full `showView`+`renderDM`+`loadConversations` re-fetch. `navigate()`'s `'chat'` case and `validViews` entry are gone (general chat isn't a distinct view/`S.view` value any more); `#chat` still works as a hash deep-link, handled specially in `handleHashNavigation()` (`navigate('dm')` + `openGeneralChat()`).
      - leaderboard.js — leaderboard.html controller: fetches `/api/leaderboard`, paginated "Load more", highlights the logged-in user's own rank
      - console-care.js — console-care.html: thin wrapper calling `initModelDirectory()` from `modules/model-directory.js` (no page-specific logic left — the directory rendering itself moved into that shared module so console-modding.js could reuse it).
      - console-modding.js — console-modding.html: thin wrapper calling `initModelDirectory()` from `modules/model-directory.js`, same as console-care.js.
      - console-model.js — console-model.html: reads `?code=` from the URL, looks it up in `await loadModels()` (`../data/console-models.js`, DB-backed since 2026-09-06 — `render()` is `async` and awaits this before calling `findModel(models)`, which now takes the loaded array as a param instead of closing over a static import), fills in the plate/title/note, sets `<title>`/meta description dynamically; shows a "model not found" fallback if the code doesn't match. **Admin "Edit model" (2026-09-06)**: `initModelAdminEditButton()` anchors a trigger (`.model-edit-trigger`) right after `#model-note`; `openModelEditor()` opens a small modal (mfr/console/code/note, reusing `model-directory.js`'s `.model-directory-add-*` modal classes) that `PUT`s to `/api/console-models/:id` — if `code` changed, the page navigates to `console-model.html?code=<newCode>` (preserving `?from=`) since the URL's `?code=` is this page's identity key and would otherwise go stale; the backend cascades the rename into `console_tutorials`/`console_mod_tutorials` so existing disassembly/modding content isn't orphaned (see `routes/console-models.js`). Shared helpers `renderTutorialMarkup(data, fieldNames)` (pure HTML-building for the read view) and `stepEditorRow(step)` (heading/description/photo-upload row, wired to `POST /api/uploads/presign` `kind: 'tutorial'` → direct PUT to storage, mirroring community.js's `submitPhotoUpload` flow) are used by both sections below. `createTutorialSection({idPrefix, titleField, introField, stepsField})` builds the **disassembly** section — single row, unchanged behaviour, reads/writes `GET`/`PUT`/`DELETE /api/console-tutorials/:code`. `createModTutorialSection()` builds the **modding guide** section — holds an array of `(flash_type, firmware_version)` combos from `GET /api/console-tutorials/:code/mod?lang=${I18nModule.lang}` (2026-09-06: `lang` param added so a combo with a `console_mod_tutorials_translations` row for the current language renders localized; the existing `window.addEventListener('cn:language-changed', render)` already re-fetches on language switch, so no extra wiring was needed) plus a `staticOptions` menu looked up from `console-models.js`'s `MOD_OPTIONS` by the model's `console` field, `flashTypes` narrowed per real model code via `flashTypesForModel()` (2026-09-08, see `console-models.js`'s entry above — set via `applyCombos(rows, consoleName, code)`, `code` param added the same day; import bumped to `?v=20260908`, and `console-model.html`'s own `<script>` tag likewise); `flashTypes()`/`versionsForFlash()` union the two so the `#mod-flash-select`/`#mod-version-select` dropdowns are populated (and visible) even for combos nobody has written yet, auto-selecting the first combo. Saves per-combo edits/new-combo adds via `PUT` and deletes via `DELETE /api/console-tutorials/:code/mod/:flashType/:version`; the "add new combo" editor renders `<select>`s pre-filled with whatever combo is currently browsed when a static/derived option list exists, else free-text inputs (`newComboFieldsMarkup()`/`wireNewComboFields()`). **Translation editing (2026-09-06)**: `openEditor`/`save`/`del`/`renderAdminControls` all branch on `I18nModule.lang` — EN keeps the original edit/add-combo/delete-combo behaviour untouched; any other language swaps in a single "Edit/Add translation" button (`renderAdminControls`, label from the combo's `has_translation`), which opens the same editor (prefilled from the already-effective, translation-or-EN-fallback `combos` entry — no separate raw fetch needed) but `save()` PUTs to `.../translations/:lang` instead of the base route, and an in-editor "Remove translation" button (shown only when `has_translation` is true) `DELETE`s just that language's row via `del()` then calls the new `reloadCombos()` helper to refresh from the server (the local `combos` cache no longer has the EN values once overwritten by a translation, so re-fetching is simpler than reconstructing them). "Add new combo" is hidden entirely outside EN — flash type/firmware version are structural identifiers, not translatable content. **Cache-bust fix (2026-09-06, same day as the MODELS→DB move)**: Andrei hit a real production bug where a model page rendered completely empty (blank plate, both tutorial sections stuck on their static "Coming soon" default) — root cause was the well-known long-JS-cache issue (see CLAUDE.md's Gotchas) hitting the breaking export change in the same commit: a stale-cached `console-model.js`/`model-directory.js` (calling `MODELS.find()`/`.filter()`) paired with a freshly-served `console-models.js` (no more `MODELS` export) throws immediately, so nothing after that point in `render()` — including the "not found" fallback — ever runs. Fixed by adding `?v=20260906` to every reference to a file whose exports changed that day: the `console-models.js` import specifier in both `console-model.js` and `model-directory.js`, the `model-directory.js` import specifier in `console-care.js`/`console-modding.js`, and the outer `<script src>` tags for `console-model.js` (console-model.html), `console-care.js`/`console-modding.js` (their own HTML pages), `lesson.js` (lesson.html), and `console-detail.js` (bulk-applied across all 52 `consoles/*.html` pages via a `sed` pass, verified 52/52 and spot-checked).
      - comparatie.js — comparison page ES module: loads console data via API, renders comparison UI; reads `?a=&b=` on load to preselect a pair, `pushState`s the URL on selection change (no reload), and dynamically rewrites `<title>`/meta description/canonical/OG tags per pair for SEO — canonical always points at the pair's canonical order (`canonicalOrder()`, sorted by index in `consoles-en.json`) so "A vs B" and "B vs A" consolidate to one URL instead of splitting ranking signal; `popstate` listener re-syncs on back/forward; bare `comparatie.html` (no params) keeps the generic static title/meta as the hub page
      - console-detail.js — console detail page ES module: loads specs from `data-loader.js`, renders into page; also injects schema.org JSON-LD (`<script id="cn-jsonld">`, `Product` + `BreadcrumbList` via `buildStructuredData`/`injectStructuredData`) built from the same consola object rendered on-page — specs go into `additionalProperty`, and `aggregateRating` is added only once `/ratings/:id` returns a real `count > 0` (no fabricated ratings/offers). **Admin edit modal (2026-09-06)**: `initAdminEditButton()` inserts a small admin-only trigger (`.console-edit-trigger`) next to the hero `<h1>` (survives `renderHero()`'s `h1.textContent` reset since it's a sibling, not a child); `openConsoleEditor()` builds a full-content modal — name/manufacturer/generation/release/image (upload via `POST /uploads/presign` `kind: 'console'`, admin-gated there too)/hardware models (add/remove rows)/all six spec groups (cpu/gpu/memory/storage/output_video/technologies, reusing the SAME `spec_label_*`/`spec_*_title` i18n keys already used to *display* those specs, so the form needed hardly any new translation strings)/advantages+disadvantages (add/remove rows)/history (raw HTML textarea, admin-trusted like `content_html` elsewhere) — on save, `PUT /api/consoles/:id` (EN only for now), then re-runs `renderHero`/`renderHistory`/`renderSpecs`/`injectStructuredData` with the returned object so the page updates with no reload, and calls `invalidateCache()` (`data-loader.js`) so a later language switch / other page doesn't serve stale cached data. Deliberately does NOT expose the variable-shaped `dimensions` field (present only on some consoles, several incompatible shapes) — the backend route merges onto the existing row so it survives a save untouched regardless.
      - cookies.js — cookie consent banner logic for cookies.html (reads/writes consent to localStorage)
      - course.js — course.html controller: loads course/module/lesson list, access control for non-starter courses
      - help.js — help.html: FAQ accordion, category filter, search
      - index.js — index.html: fetches `/api/leaderboard?limit=5` and renders the "Top Contributors" preview list
      - invata.js — invata.html: fetches real course progress from API, updates course card progress bars
      - lesson.js — lesson.html controller: loads lesson content/quiz, tracks progress, comments/reactions. **Admin edit modal (2026-09-06)**: `initLessonAdminEditButton()` appends a small admin-only trigger (`.lesson-edit-trigger`) to `.lsn-hero__inner` (survives `renderLesson()`'s `.lsn-title` textContent reset — different element entirely); `openLessonEditor()` builds a full-content modal — title/content_html (raw HTML textarea, admin-trusted like `content_html` elsewhere)/quiz questions (add/remove question cards, each with add/remove options and a radio-per-question to mark the correct one, `readLessonEditQuiz()` reads it back into the `{question, options, correct_option, explanation}` shape the backend expects, dropping any question left without ≥2 non-empty options). On save: `PUT /lessons/:id` then `PUT /lessons/:id/quiz`, then re-runs `renderLesson()`/`renderQuiz()` with the returned data (no reload) using the same `currentCourse`/`currentLsnIdx` module state `init()` already tracks, so the "Module X, Lesson Y" title prefix stays correct post-edit.
      - login.js — login.html: server/local login, Google OAuth redirect handling, 2FA verification, resend verification. `?error=account_banned` now prefers a `?message=...` query param (the real "why + until" text, set by `google-auth.js`) over the old generic `errorMessages` map entry when present (2026-09-01)
      - profile.js — profil.html Settings controller: account, profile/privacy, security, notifications, appearance tabs; admin-only reports tab (`loadAdminReports()`) adds per-report moderation buttons (ban/mute/delete-content) alongside the existing status-change actions; **fix (2026-09-01)**: `STATUS_ACTIONS['resolved']` no longer offers Dismiss alongside Reopen (once ban/mute/delete already acted, only Reopen makes sense) — `moderationAction()`'s `mute-author` branch was also still setting the local status to the pre-redesign `'reviewed'` instead of `'resolved'` (server already sends `'resolved'`), so the card briefly showed the wrong badge/actions until a full reload; admin-only analytics tab (`loadAdminAnalytics()`) renders stat tiles + a signups-per-day bar list from `GET /api/admin/stats`. Marketplace Connections panel is data-driven off `MARKETPLACES` (array of `{id, name}` — `bindEvents()`/`loadMarketplaceAccounts()` iterate it to wire connect/disconnect/sync buttons generically) — intentionally **empty** as of 2026-07-23 (eBay deactivated alongside OLX, which was already excluded); the generic `connectProvider()`/`disconnectProvider()`/`syncProvider()` functions are untouched/ready, just unreachable with nothing in the array to bind to. Backend eBay integration (`routes/ebay.js`, `providers/EbayProvider.js`) is completely untouched by this — only the frontend entry point is off. Appearance tab's theme cards (`.theme-card[data-theme-value]`, markup lives in `profil.html`) are now `dark`/`light`/`system` (2026-07-25, "Default" card removed) — `resolveThemeValue()` mirrors `profile-dropdown.js`'s `resolveTheme()` so picking "System" here resolves/stores identically to picking it from the navbar dropdown.
      - register.js — register.html: password strength indicator, username availability check, Turnstile CAPTCHA widget, submit, Google OAuth
      - request-password-reset.js — request-password-reset.html: submits reset request to API, shows result
      - reset-password.js — reset-password.html: validates token from URL, submits new password (`?mode=set` variant)
      - setup-username.js — setup-username.html: first-time username selection with live availability check
      - statistici.js — statistici.html: personal progress dashboard (achievements, visits, friends, favorites, owned consoles); wires the level-card and per-achievement share buttons via `utils/share.js`
      - user-profile.js — user-profile.html: public profile view, progress/achievements/favorites, friend-request controls; adds a "share profile" button and a read-only seller-rating stat tile (`loadSellerRatingSummary()`, only shown if the user has seller reviews); the "Active listings" grid (`loadUserListings()`) shows a "Restored by CNote" badge for official listings; level badge renders `profile.level` (real XP-based level from `GET /users/:username`, via `AchievementsModule.computeLevel`) — hidden if `null` (stats hidden), no longer estimated from friend/favorite/owned counts. **Report-profile button fix (2026-09-01)**: was `report_btn_trigger_profile` (already `'⚑ Report profile'`) with an extra literal `'⚑ '` prefixed on top — doubled the flag; also had no visible pill styling (`report-trigger-btn`'s bare class is just small gray text, no background/border) — now `user-action-btn user-action-btn--remove report-trigger-btn`, matching the friend-action buttons next to it. **Listing-image fallback fix (2026-09-01)**: the Active listings grid's no-photo fallback now uses `NO_IMAGE_PLACEHOLDER` (`utils/no-image-placeholder.js`) instead of the never-committed `/assets/images/graphics/no-image-placeholder.jpg` (confirmed 404 in production) — listings without a photo were showing the browser's broken-image icon instead of a placeholder
      - verify-success.js — verify-success.html: verifies email token from URL, shows result, handles resend (forced English)
    - **utils/**
      - confirm-modal.js — custom confirm() replacement modal (`confirmModal()`), CSP-safe DOM building. **`promptModal()`/`alertModal()` (2026-09-01, nou)** — same singleton-overlay pattern, replace `prompt()`/`alert()` the same way (Andrei: native browser dialogs looked out of place). Used by: `profile.js`'s Admin Reports ban-temp/mute-hours pickers (were `prompt()`), `user-profile.js`'s equivalent profile-page ban/mute widget (were `prompt()` + a plain native `confirm()` that had an explicit "acceptable trade-off, not imported here" comment — no longer true, now goes through the same `confirmModal()`), and `chat.js`'s send-failure/mute-rejected message (was a plain `alert()`)
      - share.js — `shareOrCopy({title, text, url})`: native Web Share sheet with clipboard-copy fallback; used by user-profile.js, statistici.js, and community.js's listing detail
      - date-picker.js — custom styled calendar dropdown replacing native/hidden date inputs (`createDatePicker()`)
      - dom.js — small DOM utility helpers: selectors, event handlers, class management
      - no-image-placeholder.js — exports `NO_IMAGE_PLACEHOLDER`, an inline SVG data URI shown for a marketplace listing with no photos (2026-09-01, new). Replaces every call site's previous `/assets/images/graphics/no-image-placeholder.jpg` reference — that file was never actually committed to the repo (confirmed 404 in production), so listings without a photo showed the browser's broken-image icon instead of a placeholder. Used by `community.js` (marketplace grid + similar-listings), `home.js` (My Listings / favorited / trending previews, 3 spots), and `user-profile.js` (public profile's Active listings grid)
    - config.js — exports `API_BASE_URL`, overridable via `window.CN_API_BASE_URL` for split frontend/API deployments
    - cookie-consent.js — cookie consent banner + conditional Google Analytics (`gtag`) loader
    - main.js — main JS entry point: imports and initializes Navigation/Animations/ContactForm/Diacritics/Search/ProfileDropdown modules
    - redirect.js — root `index.html` redirect helper, keeps that file free of inline scripts
    - reset-database.js — frontend entry shim, `require()`s the real logic in `backend/js/reset-database.js`
  - manifest.json — PWA manifest (name, icons, theme/background color), linked from every page's `<head>`
  - robots.txt — crawler rules; disallows login/register pages
  - sitemap.xml — frontend-specific sitemap (mirrors root `sitemap.xml`); covers all public pages: main pages, 52 console pages, 10 help sub-pages, 24 legal-file language variants, plus 1326 `comparatie.html?a=&b=` console-comparison-pair URLs (every unique pair of the 52 consoles, one entry per pair in canonical order only — see `comparatie.js`'s `canonicalOrder()` — to avoid submitting both "A vs B" and "B vs A" as separate/duplicate URLs); deliberately excludes `https://consolenotebook.com/` itself (pure 302 redirect — Google's guidance is to never list a redirecting URL in a sitemap). Every public page now has a self-referencing `<link rel="canonical">` (94 total: legal files already had them; index.html/home.html/evolutie/comparatie/community/community-welcome-page/invata/help + 10 help sub-pages + 52 console pages added in this pass); includes `console-care.html`, `console-modding.html`, and (2026-07-23) `articles.html` (daily changefreq — individual articles aren't enumerated, same precedent as console-model.html); 1424 `<url>` entries total. Regenerate the comparison-pair block if consoles are added/removed from `consoles-en.json` (id list + pair count must stay in sync).
- .gitignore — repo-wide ignore rules (`.env*` except `.env.example`, `node_modules/`, etc.)
- CLAUDE.md — Claude Code guidance: architecture, conventions, and rules for this repo
- index.html — root redirect shim to `frontend/html/pages/`; not the real app entry point
- LICENSE — project license
- package.json — root scripts: `install:server`/`postinstall`, `start`/`start:server`, `dev:server`, `reset-db`, `import-consoles`
- railway.toml — Railway deploy config: `startCommand` is just `npm start` now (2026-09-06 — used to run `import-consoles.js` first on every deploy; that's what forced console-encyclopedia edits to always go through the JSON files, since any DB row would get overwritten on the next deploy. See `consoles.js`'s `PUT /:id` and CLAUDE.md's "Console encyclopedia data" note for the new DB-is-live-source-of-truth architecture), healthcheck at `/api/health`
- README.md — project overview: what Cnote Bakery / Console Notebook is
- sitemap.xml — root-level XML sitemap for search engines (kept in sync with `frontend/sitemap.xml`)
